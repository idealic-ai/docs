# 51: Промпт/Синхронизация

> [!DEFINITION] Документ о Синхронизации
> Это автоматически созданный отчёт, который подводит итоги обсуждения. В нём записано, о чём все договорились, какие споры решили и что конкретно нужно сделать.
>
> Sidenote:
> - Смотри определение в документе :term[22: Компания/Синхронизация]{href="./22_document_alignment.md"}.
> - Раньше назывался: Черновик Эволюции

## Правила, которые нельзя нарушать

**СТРОГО СЛЕДУЙ ЭТИМ ПРАВИЛАМ:**

1.  **Временный файл:** Ты МОЖЕШЬ создать файл `{OUTPUT_DIR}/{FILENAME}.ndjson` для хранения комментариев. Никаких других временных файлов создавать нельзя.
2.  **Загрузка за один раз:** Загружай все нужные данные (комментарии) одной командой для каждого источника. НЕ пытайся делать это по частям или повторять загрузку.
3.  **Источник для проверки:** Всегда проверяй всё по тем данным, которые у тебя уже есть (в скачанном JSON). Никогда не скачивай их заново для проверки.
4.  **Язык:** Итоговый документ ОБЯЗАТЕЛЬНО должен быть на **русском** языке (кроме кода и технических терминов).
5.  **Полнота:** Абсолютно каждый комментарий должен быть учтён в Отчёте о покрытии.
6.  **Без перезапуска:** Если ты нашёл проблему (например, чего-то не хватает), НЕ начинай всё сначала. Исправь только эту конкретную ошибку и продолжай работу.
7.  **Анализ «в уме»:** НЕ используй для анализа, подсчётов или выводов сторонние программы (скрипты Python/Shell). Всю работу ты должен делать «в уме», используя свои внутренние возможности.

## Зачем это нужно

Документ о Синхронизации — это мостик между обсуждением (в Pull Request) и реальными действиями. Он собирает все итоги и договорённости, которые появились во время проверки кода.

## Инструкция для Агента

Когда пользователь просит создать Документ о Синхронизации, ты **ОБЯЗАН** сначала разобраться с входными параметрами.

### 1. Сбор параметров

**Вот что тебе нужно узнать:**

```json
{
  "type": "object",
  "properties": {
    "since_date": {
      "type": "string",
      "description": "Дата в формате ГГГГ-ММ-ДД. Если не указана, берём неделю назад.",
      "default": "{ONE_WEEK_AGO}"
    },
    "repo": {
      "type": "string",
      "description": "Название репозитория. Если не указано, берём текущий.",
      "default": "{CURRENT_REPO}",
      "example": "idealic-ai/platform"
    },
    "pr_number": {
      "type": "integer",
      "description": "Номер Pull Request. ЭТО ОБЯЗАТЕЛЬНО.",
      "required": true
    },
    "output_dir": {
      "type": "string",
      "description": "Папка для сохранения файла. По умолчанию 'docs'.",
      "default": "alignments"
    },
    "filename": {
      "type": "string",
      "description": "Имя файла. По умолчанию — дата. Расширение указывать не нужно.",
      "default": "{SINCE_DATE}"
    }
  }
}
```

**Шаг 0: Уточнение параметров**

1.  Прочитай запрос пользователя и найди эти значения.
2.  **Если чего-то не хватает:** Если `pr_number` не указан, **ОСТАНОВИСЬ** и спроси его.
3.  **Значения по умолчанию:** Если другие параметры не указаны, используй стандартные.

**Шаг 1: Отчёт о параметрах (Твой первый ответ)**
Твой самый первый ответ **ДОЛЖЕН** выглядеть так:

```text
> Настройки синхронизации:
--------------------------------
[x] Репозиторий:   {repo}
[x] PR номер:      {pr_number}
[x] Начиная с:     {since_date}
[x] Папка вывода:  {output_dir}
[x] Имя файла:     {filename}
--------------------------------
Начинаю анализ...
```

### 2. Получение данных

**ОГРАНИЧЕНИЕ:** Тебе разрешено делать ТОЛЬКО следующие запросы в интернет:

1.  **HTTP GET** через curl, чтобы скачать файл `https://idealic.academy/raw/simple-ru/company/02_process.md`
2.  **HTTP GET** через curl, чтобы скачать файл `https://idealic.academy/raw/simple-ru/company/50_prompt_truth.md`
3.  Один запрос к **GitHub API**, чтобы узнать автора Pull Request.
4.  Один запрос к **GitHub Comments API**, чтобы скачать все комментарии (команда ниже).

**Шаг 1: Скачай обязательные документы**
Скачай файлы по этим ссылкам. Обработай каждый из них.

```bash
# 1. Документы про Процесс и Правду
curl https://idealic.academy/raw/simple-ru/company/02_process.md
curl https://idealic.academy/raw/simple-ru/company/50_prompt_truth.md

# 2. Определение синхронизации
curl https://idealic.academy/raw/simple-ru/company/22_document_alignment.md
```

**Шаг 2: Получи детали PR (чтобы узнать роли)**
Запроси информацию о Pull Request, чтобы узнать, кто его автор.

```bash
gh api "repos/{OWNER}/{REPO}/pulls/{PR_NUMBER}" --jq '{author: .user.login, title: .title}'
```

**Шаг 3: Скачай комментарии**

1.  **Скачай в файл:** Выполни ровно эту команду, чтобы сохранить комментарии в `{OUTPUT_DIR}/{FILENAME}.ndjson`.

    ```bash
    gh api "repos/{OWNER}/{REPO}/pulls/{PR_NUMBER}/comments?since={SINCE_DATE}&per_page=100" \
    --paginate \
    | jq -s '
        add
        | group_by(.in_reply_to_id // .id)
        | map(sort_by(.created_at))
        | sort_by(.[0].created_at)
        | flatten
        | to_entries
        | map(.value + {index: ("§" + ((.key + 1) | tostring))} | del(.html_url))
        | group_by(.in_reply_to_id // .id)
        | sort_by(.[0].index | ltrimstr("§") | tonumber)
        | map(.[0] as $root | [$root] + (.[1:] | map(del(.diff_hunk))))
        | map(map({
        id,
        body,
        user: .user.login,
        created_at,
        diff_hunk: (.diff_hunk | if length > 200 then .[:200] + "..." else . end),
        in_reply_to_id,
        reactions: (.reactions | del(.total_count, .url) | with_entries(select(.value > 0))),
        index
        }))
        | map(map(del(.in_reply_to_id)))
        | .[]
    ' -c > {OUTPUT_DIR}/{FILENAME}.ndjson
    ```

### 3. Управление процессом

**Шаг 0: Составь план**

Ты **ОБЯЗАН** создать список дел с помощью инструмента `todo_write`.

**План должен быть таким:**

1.  **Этап 1: Подготовка и сбор данных** (ожидает)
2.  **Этап 2: Загрузка в память** (ожидает)
3.  **Этап 3: Общий обзор** (ожидает)
4.  **Этап 4: Намерения** (ожидает)
5.  **Этап 5: Отчёт о покрытии** (ожидает)
6.  **Этап 6: Твоя оценка** (ожидает)
7.  **Этап 7: Проверка и очистка** (ожидает)

### 4. Как нужно думать: Умный анализ

**Метод: Умный анализ**

- :term[02: Компания/Процесс]{href="https://idealic.academy/en/company/02_process.md/"}
- :term[50: Промпт/Правда]{href="https://idealic.academy/en/company/50_prompt_truth.md/"}

**Ограничение:** Итоговый документ должен быть на **русском** языке.

**Главное — ничего не упустить:**

-   **Сохраняй детали:** Не теряй технические инструкции.
-   **Одна задача — одно намерение:** Каждое «Намерение» должно описывать одно конкретное техническое изменение.
-   **Покрытие:** Учти абсолютно каждый комментарий.

### 5. Процесс создания документа

#### 5.1. Этап 1: Подготовка и сбор данных

1.  **Собери данные:** Документы, информацию о PR, комментарии.
2.  **Создай файл-шаблон:**
    -   Создай файл `{OUTPUT_DIR}/{FILENAME}.md` с **ТОЧНО** таким содержимым:

    ```markdown
    # Синхронизация: {DATE}

    - Статус: Проект соглашения
    - Автор: {PR_AUTHOR}
    - Источник: {PR_LINK}
    - Период: {SINCE_DATE} - {NOW}

    > [!WARNING] АВТО-ГЕНЕРАЦИЯ: НЕ ПРАВИТЬ, НЕ КОММИТИТЬ
    > Этот документ — **инструмент синхронизации**. Он служит:
    >
    > 1. **Валидатором:** Проверяет, что 100% комментариев услышаны.
    > 2. **Синтезатором:** Превращает дискуссию в готовый план (Consensus).
    > 3. **Промптом:** Дает инструкции для обновления Proposal.
    >
    > Если документ неверен, **не правьте текст руками**. Добавьте комментарии в PR и **перегенерируйте**, чтобы замкнуть цикл обратной связи.
    >
    > **НЕ ДОБАВЛЯЙТЕ ЭТОТ ФАЙЛ В GIT.**
    >
    > - Подробнее: [22: Alignment](https://idealic.academy/raw/simple-ru/company/22_document_alignment.md)
    > - В Git попадают только: [Proposal](https://idealic.academy/raw/simple-ru/company/21_document_proposal.md) и [Specification](https://idealic.academy/raw/simple-ru/company/20_document_spec.md).

    ## Обзор

    {{OVERVIEW_PLACEHOLDER}}

    ## Список Намерений (Consensus)

    {{INTENTS_PLACEHOLDER}}

    {{QUESTIONS_PLACEHOLDER}}

    ## Отчет о покрытии

    {{COVERAGE_PLACEHOLDER}}

    ## Оценка LLM

    {{OPINION_PLACEHOLDER}}

    ## Инструкции для Агента (Next Step)

    > [!IMPORTANT] Context Loading
    > Before proceeding with updates to the Proposal or Specification, you **MUST** load the following context files to ensure full alignment with company standards:
    >
    > 1. `curl https://idealic.academy/raw/simple-ru/company/02_process.md` (Process)
    > 2. `curl https://idealic.academy/raw/simple-ru/company/50_prompt_truth.md` (Truth)
    > 3. `curl https://idealic.academy/raw/simple-ru/company/20_document_spec.md` (Spec)
    > 4. `curl https://idealic.academy/raw/simple-ru/company/21_document_proposal.md` (Proposal)
    > 5. `curl https://idealic.academy/raw/simple-ru/company/22_document_alignment.md` (Alignment)
    ```

#### 5.2. Этап 2: Загрузка в память

1.  **Проанализируй данные:**
    -   **Посчитай ветки обсуждений:** Выполни `wc -l {OUTPUT_DIR}/{FILENAME}.ndjson` (каждая строка — это одна ветка).
    -   **Посчитай комментарии:** Выполни `jq 'length' {OUTPUT_DIR}/{FILENAME}.ndjson | awk '{s+=$1} END {print s}'`.
    -   **Отчитайся:** Напиши эти цифры в чат (например: «Найдено 45 веток обсуждений, содержащих 150 комментариев»).

2.  **Прочитай файл в память:** Используй свои обычные инструменты, чтобы прочитать `{OUTPUT_DIR}/{FILENAME}.ndjson`.
    -   **Читай по частям (обязательно):** Читай файл кусками по 25 строк, пока он не закончится.

#### 5.3. Этап 3: Общий обзор

**Анализ:**
Ответь на эти 8 вопросов в виде списка:

1.  **Чего хотел проверяющий:**
2.  **С чем согласился автор:**
3.  **С чем автор НЕ согласился:**
4.  **Общий смысл обсуждения:**
5.  **Что было понято не так:**
6.  **Какие вопросы остались без ответа:**
7.  **Какие новые идеи появились:**
8.  **Какие ещё документы это затрагивает:**

**Создание текста:**

-   Замени `{{OVERVIEW_PLACEHOLDER}}` на свои ответы.

#### 5.4. Этап 4: Намерения

**Анализ:**
Цель: **разделить всё на мельчайшие задачи.**

-   **Правило деления:** В описании намерения не должно быть союза «И». Если задача сложная, раздели её на несколько.
-   **Атомарность:** Одно Намерение = одно конкретное техническое изменение.

**Создание текста:**

-   Замени `{{INTENTS_PLACEHOLDER}}`.
-   **Используй этот шаблон:**

  ````markdown
  ### {N}. {Краткий заголовок на русском}

  - **Категория:** {Логика / Архитектура / ...}
  - **Намерение:** {Одна главная мысль}
  - **Что предлагал автор сначала:** {Первоначальная идея}
  - **Что предложил проверяющий:** {Его совет}
  - **Что решил автор в итоге:** {Финальное решение}
  - **Реакция проверяющего:** {Что он ответил на решение}
  - **Почему так решили:** {Причина}
  - **Состояние:** {Согласовано / Непонятно / ...}
  - **Итог:** {Кратко: идея изменилась? договорились?}

  > [{Имя проверяющего}]({ID}): "{Краткий пересказ его слов}"
  >
  > [{Имя автора}]({ID}): "{Краткий пересказ его слов}"

  ```{язык}
  {1-3 строки кода из diff}
  ```
  ````

-   Замени `{{QUESTIONS_PLACEHOLDER}}`.

#### 5.5. Этап 5: Отчёт о покрытии

**Анализ:**

-   **Проверка:** Прочитай список. Сравни его с исходным файлом JSON.
-   **Учти каждый комментарий:** Убедись, что каждый номер (например, `§1`) есть в отчёте.

**Создание текста:**

-   Замени `{{COVERAGE_PLACEHOLDER}}`.
-   **ВАЖНО:** Не создавай таблицу по частям. Ты должен создать **ВСЮ** таблицу за один раз командой `search_replace` после того, как все данные загружены.
-   **Используй этот шаблон:**

  Сначала добавь заголовок, а потом создай **ЕДИНУЮ** таблицу. Группируй комментарии по дате, добавляя строку с датой для каждого нового дня.

  ```markdown
  ---

  ## Отчет о покрытии

  | Индекс        | Дата/Время     | Автор  | Заголовок (кратко) | Намерение | Реакция  |
  | ------------- | -------------- | ------ | ------------------ | --------- | -------- |
  |               | **{Дата}**     |        |                    |           |          |
  | [{Idx}]({ID}) | {дд.ММ ЧЧ:мм}  | {User} | {4-6 слов}         | #{N}      | {Эмодзи} |
  | [{Idx}]({ID}) | {дд.ММ ЧЧ:мм}  | {User} | └ {4-6 слов}      | #{M},#{N} | {Эмодзи} |
  |               | **{След.Дата}**|        |                    |           |          |
  | [{Idx}]({ID}) | {дд.ММ ЧЧ:мм}  | {User} | {4-6 слов}         | -         | {Эмодзи} |
  | [{Idx}]({ID}) | {дд.ММ ЧЧ:мм}  | {User} | ├ {4-6 слов}      | #{N}      | {Эмодзи} |
  | [{Idx}]({ID}) | {дд.ММ ЧЧ:мм}  | {User} | └ {4-6 слов}      | #{N}      | {Эмодзи} |
  | [{Idx}]({ID}) | {дд.ММ ЧЧ:мм}  | {User} | {4-6 слов}         | -         | {Эмодзи} |
  ```

  **Правила:**
  1.  Используй `index` (e.g. `§1`) из JSON-файла.
  2.  **Ничего не пропускай:** У каждого комментария должна быть своя строка. Не объединяй их (например, `§1-§5`).
  3.  **Показывай ответы:** Используй значки `├` и `└` для ответов на комментарии.
  4.  **Статусы (Эмодзи):** ❓(Без ответа), ⚠️(Предупреждение), 🚧(В работе), 🗑️(Удалено), 🗣️(Ответ), 💡(Идея), 🤝(Согласовано).
  5.  **Сортировка:** **Строго по номеру индекса (§1, §2...).** Данные уже отсортированы, ты должен сохранить этот порядок.
  6.  **Группировка:** Вставляй строку с датой `| | **{Дата}** | | | | |` только тогда, когда меняется **дата начала ветки обсуждения**. Не разделяй одну ветку на разные даты, даже если ответы на неё приходили в другие дни. Вся ветка должна быть под датой своего первого комментария.

#### 5.6. Этап 6: Твоя оценка

**Создание текста:**

-   Замени `{{OPINION_PLACEHOLDER}}`.
-   **Содержание:** Напиши своё мнение о качестве обсуждения, чему можно научиться и какие вопросы у тебя остались.

#### 5.7. Этап 7: Проверка и очистка

1.  **Подумай ещё раз:** Перепроверь, всё ли ты учёл.
2.  **Исправь ссылки (хитрость для экономии памяти):**
    -   **Контекст:** Раньше мы использовали короткие номера комментариев (например, `12345678`) вместо полных ссылок, чтобы не тратить твою память. Теперь их нужно превратить в настоящие ссылки, по которым можно кликнуть.
    -   **Что делать:** Замени `(ID)` на `(https://github.com/...#issuecomment-ID)`.
    -   **Действие:** Выполни команду `sed` ниже, чтобы быстро заменить все короткие номера на полные ссылки GitHub.
    -   **Команда:**
      ```bash
      sed -i '' 's|](\([0-9]\{7,\}\))|](https://github.com/{OWNER}/{REPO}/pull/{PR_NUMBER}#issuecomment-\1)|g' {OUTPUT_DIR}/{FILENAME}.md
      ```
3.  **Прочитай файл:** Убедись, что все временные метки вроде `{{...}}` исчезли.
4.  **Уборка:** Удали файл `{OUTPUT_DIR}/{FILENAME}.ndjson`. Он больше не нужен.

### 6. Финальный результат

Проверь себя по этому списку:

- [ ] **Данные скачаны**
- [ ] **Документ создан**
- [ ] **Комментарии проверены**
- [ ] **Ссылки исправлены**
- [ ] **Технические детали сохранены**
