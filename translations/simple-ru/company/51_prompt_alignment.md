# 51: Промпт/Согласование

> [!DEFINITION] Документ Согласования
> Это документ, который создаётся автоматически по итогам обсуждения. Он собирает в одном месте все общие договорённости, решённые споры и точные инструкции для дальнейшей работы.

> Sidenote:
> - Что это за документ, подробно описано здесь: :term[22: Компания/Согласование]{href="./22_document_alignment.md"}.
> - Раньше он назывался: Черновик Эволюции.

## Правила, которые нельзя нарушать

**СТРОГО СОБЛЮДАЙ ЭТИ ПРАВИЛА:**

1.  **Временные файлы:** Ты МОЖЕШЬ создавать файл `{OUTPUT_DIR}/{FILENAME}.ndjson` для хранения необработанных комментариев. Не создавай никаких других временных файлов.
2.  **Загрузка данных за один раз:** Загружай все нужные данные (комментарии) за один запрос к API для каждого ресурса. Не пытайся вручную переключать страницы или делать запросы в цикле.
3.  **Источник для проверки:** Всегда проверяй данные по тому, что у тебя уже есть (в JSON), никогда не запрашивай их заново для проверки.
4.  **Язык:** Итоговый документ ДОЛЖЕН быть на **русском языке** (кроме кода и технических терминов).
5.  **Полнота:** Абсолютно каждый комментарий должен быть учтён в Отчёте о покрытии.
6.  **Никаких перезапусков:** Если нашлись проблемы (например, чего-то не хватает или есть пробелы в покрытии), НЕ начинай весь процесс заново. Исправь конкретную проблему (добавь намерение, поправь формулировку) и продолжай.
7.  **Анализ «в уме» (без скриптов):** НЕ используй скрипты (Python/Shell) для анализа, подсчётов или обобщения. Вся обработка ДОЛЖНА происходить «в уме», используя скрытые возможности языковой модели (LLM).

## Цель

Документ Согласования — это мостик между обсуждением (в Pull Request) и реальной работой. Он собирает все итоги и договорённости, которые появились во время проверки кода.

## Инструкция для Агента

Когда пользователь просит создать Документ Согласования, ты **ОБЯЗАН** сначала разобраться со входными параметрами.

### 1. Сбор параметров

**Как должны выглядеть входные данные:**

```json
{
  "type": "object",
  "properties": {
    "since_date": {
      "type": "string",
      "description": "Дата в формате ГГГГ-ММ-ДД. Если не указана, берётся дата недельной давности.",
      "default": "{ONE_WEEK_AGO}"
    },
    "repo": {
      "type": "string",
      "description": "Название репозитория (организация/имя). Если не указано, берётся текущий репозиторий.",
      "default": "{CURRENT_REPO}",
      "example": "idealic-ai/platform"
    },
    "pr_number": {
      "type": "integer",
      "description": "Номер Pull Request. ОБЯЗАТЕЛЬНО.",
      "required": true
    },
    "output_dir": {
      "type": "string",
      "description": "Папка для сохранения результата. По умолчанию 'docs'.",
      "default": "alignments"
    },
    "filename": {
      "type": "string",
      "description": "Имя файла для результата. По умолчанию используется дата из since_date. Расширение указывать не нужно.",
      "default": "{SINCE_DATE}"
    }
  }
}
```

**Шаг 0: Уточнение параметров**

1.  Проанализируй запрос пользователя, чтобы найти эти значения.
2.  **Нет обязательного параметра:** Если не указан `pr_number`, **ОСТАНОВИСЬ** и спроси его.
3.  **Значения по умолчанию:** Если какие-то необязательные параметры не указаны, используй для них значения по умолчанию.

**Шаг 1: Показать настройки (Первый ответ)**
Твоим самым первым ответом **ОБЯЗАТЕЛЬНО** должен быть блок с итоговыми настройками:

```text
> Конфигурация для Согласования:
--------------------------------
[x] Репозиторий:   {repo}
[x] Номер PR:      {pr_number}
[x] Дата начала:   {since_date}
[x] Папка вывода:  {output_dir}
[x] Имя файла:     {filename}
--------------------------------
Начинаю анализ...
```

### 2. Получение данных

**ОГРАНИЧЕНИЕ:** Тебе разрешено делать ТОЛЬКО следующие внешние запросы:

1.  **HTTP GET** через curl на `https://idealic.academy/raw/simple-ru/company/02_process.md`
2.  **HTTP GET** через curl на `https://idealic.academy/raw/simple-ru/company/50_prompt_truth.md`
3.  **Запрос к GitHub API** для Pull Request (чтобы узнать автора)
4.  **Запрос к GitHub API** для комментариев (через команду, указанную ниже)

**Шаг 1: Загрузка обязательных документов**
Загрузи документы по этим ссылкам. Обрабатывай каждый документ по отдельности.

```bash
# 1. Процесс и Правда
curl https://idealic.academy/raw/simple-ru/company/02_process.md
curl https://idealic.academy/raw/simple-ru/company/50_prompt_truth.md

# 2. Определение Согласования
curl https://idealic.academy/raw/simple-ru/company/22_document_alignment.md
```

**Шаг 2: Получение деталей PR (Определение ролей)**
Запроси детали PR, чтобы узнать, кто его автор.

```bash
gh api "repos/{OWNER}/{REPO}/pulls/{PR_NUMBER}" --jq '{author: .user.login, title: .title}'
```

**Шаг 3: Загрузка комментариев**

1.  **Скачать в файл:** Выполни в точности эту команду, чтобы сохранить комментарии в файл `{OUTPUT_DIR}/{FILENAME}.ndjson`.

    ```bash
    gh api "repos/{OWNER}/{REPO}/pulls/{PR_NUMBER}/comments?since={SINCE_DATE}&per_page=100" \
    --paginate \
    | jq -s '
        add
        | group_by(.in_reply_to_id // .id)
        | map(sort_by(.created_at))
        | sort_by(.[0].created_at)
        | flatten
        | to_entries
        | map(.value + {index: ("§" + ((.key + 1) | tostring))} | del(.html_url))
        | group_by(.in_reply_to_id // .id)
        | sort_by(.[0].index | ltrimstr("§") | tonumber)
        | map(.[0] as $root | [$root] + (.[1:] | map(del(.diff_hunk))))
        | map(map({
        id,
        body,
        user: .user.login,
        created_at,
        diff_hunk: (.diff_hunk | if length > 200 then .[:200] + "..." else . end),
        in_reply_to_id,
        reactions: (.reactions | del(.total_count, .url) | with_entries(select(.value > 0))),
        index
        }))
        | map(map(del(.in_reply_to_id)))
        | .[]
    ' -c > {OUTPUT_DIR}/{FILENAME}.ndjson
    ```

### 3. Управление процессом

**Шаг 0: Составление плана**

Ты **ОБЯЗАН** создать список задач с помощью инструмента `todo_write`.

**Структура плана (обязательно):**

1.  **Этап 1: Подготовка и данные** (ожидает)
2.  **Этап 2: Загрузка контекста** (ожидает)
3.  **Этап 3: Общий обзор** (ожидает)
4.  **Этап 4: Намерения** (ожидает)
5.  **Этап 5: Отчёт о покрытии** (ожидает)
6.  **Этап 6: Оценка от LLM** (ожидает)
7.  **Этап 7: Проверка и очистка** (ожидает)

### 4. Метод: Интеллектуальный синтез

**Как работать: Интеллектуальный синтез**

- :term[02: Компания/Процесс]{href="https://idealic.academy/ru/company/02_process.md/"}
- :term[50: Промпт/Правда]{href="https://idealic.academy/ru/company/50_prompt_truth.md/"}

**Ограничение:** Итоговый документ должен быть на **русском языке**.

**Главное — ничего не упустить:**

- **Сохраняй детали:** Не теряй технические инструкции.
- **Одна мысль — одно намерение:** Каждое Намерение = Одно конкретное техническое изменение.
- **Полнота:** Учти абсолютно каждый комментарий.

### 5. Процесс создания документа

#### 5.1. Этап 1: Подготовка и данные

1.  **Загрузи данные:** Документы, информацию о PR, комментарии.
2.  **Создай файл-шаблон:**
    - Создай файл `{OUTPUT_DIR}/{FILENAME}.md` со структурой, **В ТОЧНОСТИ** повторяющей эту:

    ```markdown
    # Согласование: {DATE}

    - Статус: Проект консенсуса
    - Автор: {PR_AUTHOR}
    - Источник: {PR_LINK}
    - Период: {SINCE_DATE} - {NOW}

    > [!WARNING] АВТО-ГЕНЕРАЦИЯ: НЕ ПРАВИТЬ, НЕ КОММИТИТЬ
    > Этот документ — **инструмент синхронизации**. Он служит:
    >
    > 1. **Валидатором:** Проверяет, что 100% комментариев услышаны.
    > 2. **Синтезатором:** Превращает дискуссию в готовый план (Консенсус).
    > 3. **Промптом:** Дает инструкции для обновления Proposal.
    >
    > Если документ неверен, **не правьте текст руками**. Добавьте комментарии в PR и **перегенерируйте**, чтобы замкнуть цикл обратной связи.
    >
    > **НЕ ДОБАВЛЯЙТЕ ЭТОТ ФАЙЛ В GIT.**
    >
    > - Подробнее: [22: Согласование](https://idealic.academy/raw/simple-ru/company/22_document_alignment.md)
    > - В Git попадают только: [Proposal](https://idealic.academy/raw/simple-ru/company/21_document_proposal.md) и [Спецификация](https://idealic.academy/raw/simple-ru/company/20_document_spec.md).

    ## Обзор

    {{OVERVIEW_PLACEHOLDER}}

    ## Список Намерений (Консенсус)

    {{INTENTS_PLACEHOLDER}}

    {{QUESTIONS_PLACEHOLDER}}

    {{COVERAGE_PLACEHOLDER}}

    {{OPINION_PLACEHOLDER}}

    ## Инструкции для Агента (Следующий шаг)

    > [!IMPORTANT] Загрузка контекста
    > Прежде чем вносить изменения в Proposal или Спецификацию, ты **ОБЯЗАН** загрузить следующие файлы, чтобы полностью соответствовать стандартам компании:
    >
    > 1. `curl https://idealic.academy/raw/simple-ru/company/02_process.md` (Процесс)
    > 2. `curl https://idealic.academy/raw/simple-ru/company/50_prompt_truth.md` (Правда)
    > 3. `curl https://idealic.academy/raw/simple-ru/company/20_document_spec.md` (Спецификация)
    > 4. `curl https://idealic.academy/raw/simple-ru/company/21_document_proposal.md` (Proposal)
    > 5. `curl https://idealic.academy/raw/simple-ru/company/22_document_alignment.md` (Согласование)
    ```

#### 5.2. Этап 2: Загрузка контекста

1.  **Проанализируй данные:**
    - **Посчитай ветки обсуждений:** Выполни `wc -l {OUTPUT_DIR}/{FILENAME}.ndjson` (Каждая строка — это одна ветка).
    - **Посчитай комментарии:** Выполни `jq 'length' {OUTPUT_DIR}/{FILENAME}.ndjson | awk '{s+=$1} END {print s}'`.
    - **Отчитайся:** Чётко напиши эти числа (например, «Найдено 45 веток обсуждений, содержащих 150 комментариев»).

2.  **Прочитай данные:** Используй стандартные инструменты для чтения файла `{OUTPUT_DIR}/{FILENAME}.ndjson`.
    - **Читай по частям (обязательно):** Читай файл кусками по 25 строк, пока он не закончится.

#### 5.3. Этап 3: Общий обзор

**Анализ:**
Чётко ответь на эти 8 вопросов в виде списка:

1.  **Чего хотел проверяющий:**
2.  **С чем согласился автор:**
3.  **С чем автор НЕ согласился:**
4.  **Общий контекст:**
5.  **Недопонимания:**
6.  **Открытые вопросы:**
7.  **Новые открытия:**
8.  **Связанные документы:**

**Создание:**

- Замени `{{OVERVIEW_PLACEHOLDER}}` на результат.

#### 5.4. Этап 4: Намерения

**Анализ:**
Цель: **максимальная детализация**.

- **Правило разделения:** Никаких «И». Если в одной ветке обсуждается несколько идей, раздели их.
- **Одна мысль — одно намерение:** Каждое Намерение = Одно конкретное техническое изменение.

**Создание:**

- Замени `{{INTENTS_PLACEHOLDER}}` на результат.
- **Шаблон для заполнения:**

  ````markdown
  ### {N}. {Короткое название на русском}

  - **Категория:** {Логика / Архитектура / ...}
  - **Намерение:** {Одна главная цель изменения}
  - **Подход автора:** {Как автор хотел сделать сначала}
  - **Предложение проверяющего:** {Что посоветовали}
  - **Решение автора:** {Что автор решил сделать в итоге}
  - **Реакция проверяющего:** {Что ответили на решение}
  - **Обоснование:** {Почему решили именно так}
  - **Статус:** {Согласовано / Недопонимание / ...}
  - **Результат:** {Кратко: что изменилось? договорились?}

  > [{Имя проверяющего}]({ID}): «{Краткий пересказ его слов}»
  >
  > [{Имя автора}]({ID}): «{Краткий пересказ его слов}»

  ```{lang}
  {не больше 1-3 строк изменённого кода}
  ```
  ````

- Замени `{{QUESTIONS_PLACEHOLDER}}`.

#### 5.5. Этап 5: Отчёт о покрытии

**Анализ:**

- **Проверка:** Прочитай список. Сравни его с исходным JSON.
- **Учти каждый комментарий:** Убедись, что для каждого номера (например, `§1`) есть запись.

**Создание:**

- Замени `{{COVERAGE_PLACEHOLDER}}`.
- **КРИТИЧЕСКИ ВАЖНО:** Не создавай таблицу по частям. Ты должен сгенерировать **всю** таблицу за один вызов `search_replace` после того, как все данные будут загружены.
- **Шаблон для заполнения:**

  Создай **ЕДИНУЮ** таблицу. Сгруппируй комментарии по дате. Для каждой новой даты вставляй «строку-заголовок». Перед таблицей добавь заголовок секции:

  ```markdown
  ---

  ## Отчет о покрытии

  | Индекс        | Дата/Время     | Автор  | Заголовок (кратко) | Намерение | Реакция  |
  | ------------- | -------------- | ------ | ------------------ | --------- | -------- |
  |               | **{Date}**     |        |                    |           |          |
  | [{Idx}]({ID}) | {dd.MM HH:mm}  | {User} | {4-6 слов}        | #{N}      | {Эмодзи} |
  | [{Idx}]({ID}) | {dd.MM HH:mm}  | {User} | └ {4-6 слов}      | #{M},#{N} | {Эмодзи} |
  |               | **{NextDate}** |        |                    |           |          |
  | [{Idx}]({ID}) | {dd.MM HH:mm}  | {User} | {4-6 слов}        | -         | {Эмодзи} |
  | [{Idx}]({ID}) | {dd.MM HH:mm}  | {User} | ├ {4-6 слов}      | #{N}      | {Эмодзи} |
  | [{Idx}]({ID}) | {dd.MM HH:mm}  | {User} | └ {4-6 слов}      | #{N}      | {Эмодзи} |
  | [{Idx}]({ID}) | {dd.MM HH:mm}  | {User} | {4-6 слов}        | -         | {Эмодзи} |
  ```

  **Правила:**
  1.  Используй `index` (например, `§1`) из JSON.
  2.  **Исчерпывающе:** Для каждого комментария должна быть своя строка. Не объединяй их в диапазоны (вроде `§1-§5`).
  3.  **Для наглядности:** Используй символы `├` и `└`, чтобы показать ответы в ветке обсуждения.
  4.  **Значки-статусы (эмодзи):** ❓(Без ответа), ⚠️(Предупреждение), 🚧(В работе), 🗑️(Удалено), 🗣️(Ответ), 💡(Идея), 🤝(Согласовано).
  5.  **Сортировка:** **Строго по номеру (индексу: §1, §2...).** Ты получаешь комментарии уже отсортированными по веткам. Не меняй этот порядок.
  6.  **Группировка:** Вставляй заголовок с датой `| | **{Date}** | | | | |` ТОЛЬКО тогда, когда **дата начала новой ветки** меняется. Не разделяй одну ветку на несколько дат, даже если ответы на неё приходили в другие дни. Вся ветка должна быть под датой своего первого комментария.

#### 5.6. Этап 6: Оценка от LLM

**Создание:**

- Замени `{{OPINION_PLACEHOLDER}}`.
- **Содержание:** Напиши о качестве обсуждения, своё мнение, чему можно научиться и какие вопросы остались.

#### 5.7. Этап 7: Проверка и очистка

1.  **Глубокая проверка:** Ещё раз проверь, что ничего не упущено.
2.  **Превращение ссылок (хитрость для экономии памяти):**
    - **Контекст:** Во время работы мы специально использовали короткие номера комментариев (например, `12345678`) в ссылках вместо полных URL-адресов. Это было сделано, чтобы сэкономить память. Теперь нужно превратить эти номера обратно в рабочие ссылки.
    - **Задача:** Заменить `(ID)` на `(https://github.com/...#issuecomment-ID)`.
    - **Действие:** Выполни следующую `sed` команду, чтобы массово заменить все короткие номера на полные ссылки GitHub.
    - **Команда:**
      ```bash
      sed -i '' 's|](\([0-9]\{7,\}\))|](https://github.com/{OWNER}/{REPO}/pull/{PR_NUMBER}#issuecomment-\1)|g' {OUTPUT_DIR}/{FILENAME}.md
      ```
3.  **Прочитай файл:** Убедись, что все временные метки (placeholders) заменены.
4.  **Уборка:** Удали временный файл `{OUTPUT_DIR}/{FILENAME}.ndjson`.

### 6. Финальный результат

Проверь себя по списку:

- [ ] **Данные загружены**
- [ ] **Документ создан**
- [ ] **Комментарии проверены**
- [ ] **Временные ссылки исправлены**
- [ ] **Технические детали сохранены**
