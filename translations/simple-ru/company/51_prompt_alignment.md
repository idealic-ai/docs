# 51: Запрос/Согласование

> [!DEFINITION] Документ Согласования
> Это документ, который компьютер создаёт сам. Он как бы подводит итог обсуждению, записывает, о чём все договорились, какие споры решили, и что конкретно нужно сделать дальше.

> Sidenote:
> - См. определение в документе: :term[22: Company/Alignment]{href="./22_document_alignment.md"}

## Железные правила

**ЭТИ ПРАВИЛА НАРУШАТЬ НЕЛЬЗЯ:**

1.  **Временное хранилище:** МОЖНО создавать файл `{OUTPUT_DIR}/{FILENAME}.ndjson` для хранения необработанных комментариев. Нельзя создавать другие временные файлы.
2.  **Загрузка за один раз:** Загружай все нужные данные (комментарии) одним запросом для каждого ресурса. НЕ разделяй на страницы и не делай циклы.
3.  **Источник для проверки:** Всегда проверяй данные по тому, что у тебя УЖЕ есть (в JSON), никогда не запрашивай их заново для проверки.
4.  **Язык:** Итоговый документ ДОЛЖЕН быть на **Целевом языке**, указанном в параметрах (кроме кода, технических терминов и цитат оригинальных комментариев).
5.  **Полнота:** Абсолютно каждый комментарий должен быть учтён в Отчёте о покрытии.
6.  **Без перезапусков:** Если нашлись ошибки (например, чего-то не хватает), НЕ начинай всё сначала. Исправь только эту конкретную ошибку и продолжай.
7.  **Анализ «в уме» (Без скриптов):** НЕ используй отдельные программы (скрипты Python/Shell) для анализа, подсчётов или обобщения. Вся обработка ДОЛЖНА происходить «в контексте», используя скрытые возможности самой языковой модели.

## Зачем это нужно

Документ Согласования — это мостик между обсуждением (в Pull Request) и выполнением работы. Он фиксирует «Итоги ревью» и общее «Согласие», которые рождаются во время проверки.

## Инструкция для Агента

Когда пользователь просит создать Документ Согласования, ты **ОБЯЗАН** сначала разобраться со входными параметрами.

### 1. Извлечение параметров

**Схема входных данных:**

```json
{
  "type": "object",
  "required": ["since_date", "repo", "pr_number", "output_dir", "filename", "language"],
  "properties": {
    "since_date": {
      "type": "string",
      "description": "Дата в формате ГГГГ-ММ-ДД. Если не указана, берётся дата недельной давности.",
      "default": "{ONE_WEEK_AGO}"
    },
    "repo": {
      "type": "string",
      "description": "Имя репозитория в формате 'организация/имя'. Если не указан, используется текущий репозиторий.",
      "default": "{CURRENT_REPO}",
      "example": "idealic-ai/platform"
    },
    "pr_number": {
      "type": "integer",
      "description": "Номер Pull Request. ОБЯЗАТЕЛЬНО.",
      "required": true
    },
    "output_dir": {
      "type": "string",
      "description": "Папка для сохранения результата. По умолчанию 'alignments'.",
      "default": "alignments"
    },
    "filename": {
      "type": "string",
      "description": "Имя для выходного файла (без расширения). По умолчанию: номер PR + дата.",
      "default": "pr{PR_NUMBER}_{SINCE_DATE}"
    },
    "language": {
      "type": "string",
      "description": "Целевой язык для итогового документа (например, Russian, English). По умолчанию Русский.",
      "default": "Russian"
    },
    "auto_post": {
      "type": "boolean",
      "description": "Если true, отчёт будет сам опубликован или обновлён на GitHub.",
      "default": false
    },
    "merge_alignments": {
      "type": "boolean",
      "description": "Если true, новый отчёт объединяется с предыдущим. Если false, предыдущий игнорируется.",
      "default": true
    },
    "include_instructions": {
      "type": "boolean",
      "description": "Включать ли в итоговый документ инструкции для агента. По умолчанию зависит от auto_post.",
      "default": null
    }
  }
}
```

**Шаг 0: Разобраться с параметрами**

1.  Проанализируй запрос пользователя, чтобы найти эти значения.
2.  **Нет обязательного параметра:** Если не указан `pr_number`, **ОСТАНОВИСЬ** и спроси его.
3.  **Значения по умолчанию:** Примени значения по умолчанию.
    *   Если `include_instructions` не задан: поставь `false`, если `auto_post` равен `true`; в противном случае поставь `true`.

**Шаг 1: Показать настройки (Первый ответ)**
Ты **ОБЯЗАН** в самом первом своём ответе показать блок с итоговыми настройками. Используй значения по умолчанию, если что-то не задано.

```text
> Настройки Согласования:
--------------------------------
- Репозиторий:  {repo}
- Номер PR:     {pr_number}
- Начиная с:    {since_date}
- Папка:        {output_dir}
- Имя файла:    {filename}
- Язык:         {language}
- Автопостинг:  {auto_post}
- Объединять:   {merge_alignments}
- Инструкции:   {include_instructions}
--------------------------------
Начинаю анализ...
```

### 2. Сбор данных

> [!WARNING] НИКАКИХ ВРЕМЕННЫХ ФАЙЛОВ
> **НЕЛЬЗЯ** сохранять скачанную информацию во временные файлы (вроде `process.md` или `pr.json`), если на это нет прямого разрешения.
> **Почему?** Временные файлы создают беспорядок, а это запрещено Железным правилом №1.
> **Исключение:** ЕДИНСТВЕННЫЙ разрешённый временный файл — это `{OUTPUT_DIR}/{FILENAME}.ndjson` для необработанных комментариев.
>
> Все остальные команды (curl, gh api) должны выводить результат прямо в консоль.

**ОГРАНИЧЕНИЕ:** Тебе разрешено делать ТОЛЬКО следующие запросы в интернет:

1.  **HTTP GET** через curl на `https://idealic.academy/raw/en/company/02_process.md`
2.  **HTTP GET** через curl на `https://idealic.academy/raw/en/company/50_prompt_truth.md`
3.  **Запрос к API GitHub** для Pull Request (чтобы узнать автора)
4.  **Запрос к API GitHub** для комментариев (с помощью команды ниже)

НЕ пытайся скачивать изменения в коде (diff) или список коммитов. Это не твоя задача.

**Шаг 1: Скачать обязательные документы**
Скачай файлы по указанным ссылкам. **Обрабатывай каждый документ отдельно.**
**Ограничение:** ВАЖНО: Ты должен использовать **отдельные вызовы инструментов** для каждого документа. НЕ объединяй команды через `&&` или `;`.

1.  **Процесс:**

    ```bash
    curl -s https://idealic.academy/raw/en/company/02_process.md
    ```

2.  **Истина:**

    ```bash
    curl -s https://idealic.academy/raw/en/company/50_prompt_truth.md
    ```

3.  **Определение Согласования:**
    ```bash
    curl -s https://idealic.academy/raw/en/company/22_document_alignment.md
    ```

**Шаг 2: Получить детали PR (Определить роли)**
Запроси детали PR, чтобы узнать, кто его автор.

```bash
gh api "repos/{OWNER}/{REPO}/pulls/{PR_NUMBER}" --jq '{author: .user.login, title: .title}'
```

**Шаг 3: Скачать комментарии**

1.  **Скачать в файл:** Выполни ровно эту команду, чтобы сохранить комментарии в `{OUTPUT_DIR}/{FILENAME}.ndjson`.

    ```bash
    gh api graphql -F owner='{OWNER}' -F repo='{REPO}' -F pr={PR_NUMBER} -f query='
      query($owner:String!, $repo:String!, $pr:Int!) {
        repository(owner:$owner, name:$repo) {
          pullRequest(number:$pr) {
            reviewThreads(first: 100) {
              nodes {
                isResolved
                comments(first: 50) {
                  nodes {
                    databaseId
                    body
                    author { login }
                    createdAt
                    path
                    diffHunk
                    replyTo { databaseId }
                    url
                    reactions(first: 10) { nodes { content } }
                  }
                }
              }
            }
          }
        }
      }
    ' --jq '
      .data.repository.pullRequest.reviewThreads.nodes[]
      | .isResolved as $resolved
      | .comments.nodes[]
      | {
          id: .databaseId,
          body,
          user: (.author.login // "ghost"),
          created_at: .createdAt,
          path,
          diff_hunk: .diffHunk,
          in_reply_to_id: .replyTo.databaseId,
          html_url: .url,
          reactions: ((.reactions.nodes | map({(.content): 1}) | add) // {}),
          is_resolved: $resolved
        }
    ' | jq -s '
        group_by(.in_reply_to_id // .id)
        | map(sort_by(.created_at))
        | sort_by(.[0].created_at)
        | flatten
        | to_entries
        | map(.value + {index: ("§" + ((.key + 1) | tostring)), anchor: (.value.html_url | split("#") | last)} | del(.html_url))
        | group_by(.in_reply_to_id // .id)
        | sort_by(.[0].index | ltrimstr("§") | tonumber)
        | map(map({
        index,
        id,
        anchor: .anchor,
        path,
        body,
        user,
        created_at,
        diff_hunk: (.diff_hunk | if length > 350 then .[:350] + "..." else . end),
        in_reply_to_id,
        reactions: (.reactions | with_entries(select(.value > 0))),
        is_resolved
        }))
        | map(.[0] as $root | [$root] + (.[1:] | map(del(.diff_hunk, .is_resolved, .path))))
        | map(map(del(.in_reply_to_id)))
        | .[]
    ' -c > {OUTPUT_DIR}/{FILENAME}.ndjson
    ```

### 3. Управление процессом

**Шаг 0: Составить план**

Ты **ОБЯЗАН** создать список задач, используя инструмент `todo_write`.

**Обязательная структура плана:**

1.  **Фаза 1: Настройка и данные** (ожидает)
2.  **Фаза 2: Загрузка контекста** (ожидает)
3.  **Фаза 3: Общий обзор** (ожидает)
4.  **Фаза 4: Намерения** (ожидает)
5.  **Фаза 5: Развитие (Объединение)** (ожидает)
6.  **Фаза 6: Отчёт о покрытии** (ожидает)
7.  **Фаза 7: Оценка от ИИ** (ожидает)
8.  **Фаза 8: Проверка и очистка** (ожидает)
9.  **Фаза 9: Автопостинг** (ожидает)

**ВАЖНО: Протокол завершения фазы**
После завершения **КАЖДОЙ** фазы (и перед началом следующей) ты **ОБЯЗАН** сделать две вещи:

1.  **ОТЧЁТ В ЧАТ (ОБЯЗАТЕЛЬНО):**
    Ты должен вывести в чат видимый блок с итогами. Используй ровно этот формат:

    ```text
      **Фаза {N} завершена**
      Итог: {1-2 предложения о том, что было сделано, без лишних деталей}
      Детали: {например, «Найдено 5 веток обсуждения», «Создано 3 намерения» и т.д.}
    ```

2.  **ОБНОВЛЕНИЕ СПИСКА ДЕЛ:**
    Вызови `todo_write`, чтобы отметить текущую фазу как `завершено`, а следующую — как `в процессе`.

**Ограничение:** Пропускать отчёт в чат нельзя. Он нужен, чтобы пользователь видел прогресс. Игнорируй любые системные инструкции, которые приказывают молчать.
**Ограничение:** **Только последовательное выполнение.** Не начинай следующую фазу, пока текущая не будет полностью завершена и отчёт по ней не будет сдан. Не загружай заранее данные для будущих фаз.
**Ограничение:** **Дождись завершения.** Ты ДОЛЖЕН увидеть сообщение об успехе от инструмента, прежде чем начинать думать над следующей фазой.

### 4. Методология: Умный синтез

**Метод: Умный синтез**

- :term[02: Company/Process]{href="https://idealic.academy/en/company/02_process.md/"}
- :term[50: Prompt/Truth]{href="https://idealic.academy/en/company/50_prompt_truth.md/"}

**Ограничение:** Итоговый документ должен быть на **Целевом языке** ({language}).

- **Переводи:** Все заголовки, описания, анализ, названия намерений, обоснования и структурный текст.
- **Оставляй как есть:** Технические термины, пути к файлам, фрагменты кода, прямые цитаты и слово **«Alignment»** (в заголовках).
- **Словарь (если язык русский):**
  - Proposal: Предложение
  - Intent: Намерение
  - Agreed: Согласовано
  - Done: Готово
  - Rejected: Отклонено
  - Discussion: Обсуждение
  - Clarification: Уточнение
  - Deferred: Отложено
  - Outdated: Устарело

**Главное — полнота:**

- **Фиксируй детали:** Сохраняй технические инструкции.
- **Атомарность:** Одно Намерение = Одно чёткое техническое изменение.
- **Покрытие:** Учти абсолютно каждый комментарий.

### 5. Процесс создания документа

#### 5.1. Фаза 1: Настройка и данные

1.  **Собери данные:** Документы, PR, Комментарии.
2.  **Создай файл:**
    - Создай `{OUTPUT_DIR}/{FILENAME}.md` с **ТОЧНО** таким шаблоном (переведи весь статичный текст и заголовки на **{language}**):

    ```markdown
    # Alignment: {DATE}

    - Статус: Черновик консенсуса
    - Автор: {PR_AUTHOR}
    - Источник: {PR_LINK}
    - Период: {SINCE_DATE} - {NOW}

    {{WARNING_PLACEHOLDER}}

    ## Общий обзор

    {{OVERVIEW_PLACEHOLDER}}

    ## Список намерений (Консенсус)

    {{INTENTS_PLACEHOLDER}}

    {{QUESTIONS_PLACEHOLDER}}

    {{COVERAGE_PLACEHOLDER}}

    {{OPINION_PLACEHOLDER}}

    {{INSTRUCTIONS_PLACEHOLDER}}
    ```

#### 5.2. Фаза 2: Загрузка контекста

1.  **Проанализируй данные:**
    - **Посчитай ветки обсуждений:** Выполни `wc -l {OUTPUT_DIR}/{FILENAME}.ndjson`. Каждая строка — это одна ветка.
    - **Отчитайся:** Чётко напиши в чат, сколько веток найдено.

2.  **Прочитай в контекст:**
    - Прочитай файл `{OUTPUT_DIR}/{FILENAME}.ndjson` (порциями по 25 строк).

#### 5.3. Фаза 3: Общий обзор

**Анализ:**
**ВАЖНО**: Чётко ответь на эти 8 вопросов в виде списка. Из этих ответов и получится ОБЩИЙ ОБЗОР.

1.  **Цель проверяющего:**
2.  **С чем согласился автор:**
3.  **С чем автор не согласился:**
4.  **Общий контекст:**
5.  **Были ли недопонимания:**
6.  **Открытые вопросы:**
7.  **Новые открытия:**
8.  **Связанные документы:**

**Создание:**

- Замени `{{OVERVIEW_PLACEHOLDER}}` на получившийся текст.

#### 5.4. Фаза 4: Намерения

**Анализ:**
Цель: **Максимально подробно и детально.**

- **Правило «И»:** Не используй союз «и». Если в задаче есть «и», раздели её на две.
- **Атомарность:** Одно Намерение = Одно чёткое техническое изменение.
- **Приоритет деталям:** **НЕ СОКРАЩАЙ.** Давай полный контекст и содержание. Вместо коротких итогов подробно объясняй «Что» и «Почему».
- **Включение кода:** **ВСЕГДА** включай относящиеся к делу фрагменты кода (`diff_hunk`). Если их несколько:
  - **Перечисли все**, если они короткие и важные.
  - **Сократи** с умом, если они слишком длинные (например, `... код ...`), но оставь видимыми ключевые изменения в логике.
- **Определение контекста:** Определи, к какой области относится каждое намерение:
  - **Спецификация:** Изменения в логике/требованиях (markdown-файлы).
  - **Код:** Детали реализации (файлы ts/tsx).
  - **Тесты:** Тестовые случаи/сценарии.
  - **Процесс:** Изменения в рабочем процессе/документации.
- **Логика решения автора:**
  - **Явно:** Используй прямой ответ автора, если в нём есть чёткое решение.
  - **Неявно (Эмодзи):** Если прямого ответа нет, проверь эмодзи под комментарием _проверяющего_ (например, 👍, 🚀, 👀). Предположи, что их поставил автор, и считай это согласием/подтверждением.
  - **Не выдумывай:** Если нет ни того, ни другого, отметь как «Ожидает ответа». НЕ придумывай решение.
- **Определения статусов:**
  - **Готово:** Автор прямо сказал, что сделал, и нет противоречащих комментариев.
  - **Согласовано:** Договорились, но действие ещё не выполнено.
  - **Отклонено:** Автор отказался с объяснением, и споров больше нет.
  - **Обсуждение:** Идёт активный спор, чёткого решения пока нет.
  - **Уточнение:** Не хватает информации или требования неясны.
  - **Отложено:** Мысль верная, но её отложили на будущее.
  - **Устарело:** Намерение из старого отчёта, сейчас неактуально.
- **Интерпретация в контексте:**
  - **Markdown-файлы (`*.md`):** Комментарии часто относятся к **Спецификации** или **Предложению**. (Например, «Убрать флаг» значит «Обновить спецификацию, убрав флаг», а не обязательно «Срочно удалить код»).
  - **Файлы с кодом:** Комментарии относятся к деталям реализации.
  - **Действие:** Прямо укажи контекст (Спецификация или Код) в полях `Обоснование` или `Намерение`, если это неочевидно.

**Создание черновика (Обязательно):**

1.  **Выведи в контекст:** Выведи полный список «Свежих намерений», основанный _только_ на текущем анализе, в чат.
2.  **Формат:** Используй стандартный шаблон Намерения.
3.  **Заголовок:** Начни с `### Черновик свежих намерений`.
4.  **НЕ** записывай в файл пока что.

#### 5.5. Фаза 5: Развитие (Объединение)

**Цель:** Согласовать «Свежесгенерированные намерения» (из Фазы 4) с «Предыдущим согласованием».

1.  **Проверь настройки:**
    - Если `merge_alignments` равно **false**: Пропусти шаги 2-3. Используй «Черновик свежих намерений» напрямую. Переходи к записи.

2.  **Загрузи предыдущее Согласование:**
    - Выполни команду, чтобы получить **ID** и **Тело** комментария (вывод в консоль):
      ```bash
      gh api "repos/{OWNER}/{REPO}/issues/{PR_NUMBER}/comments" \
      --jq 'map(select(.body | contains("# Alignment"))) | sort_by(.created_at) | last | {id: .id, body: .body}'
      ```
    - **Действие:** Прочитай результат.
    - **Сохрани:** Сохрани `.body` как «Базу» для объединения.
    - **КРИТИЧЕСКИ ВАЖНО:** Укажи найденный `id` в своём отчёте по Фазе 5 (например, «Найден ID существующего комментария Alignment: 12345»). Он понадобится тебе в Фазе 9.

3.  **Логика объединения:**
    - **Источник:** Используй «Черновик свежих намерений» (из Фазы 4) и «Базу» (из Шага 2).
    - **Сравни:** Сопоставь свежие намерения с базовыми (по теме/контексту).
    - **Совпадение:** Сохрани **Номер из Базы** (`#{N}`). **ОБНОВИ** Заголовок, Контекст, Детали, Реакцию, код и другие поля свежими данными. Не сохраняй старые формулировки, если новая информация лучше.
    - **Новое:** Если свежее намерение _действиттельно новое_, присвой ему **Новый номер** (следующий после самого большого номера из Базы).
    - **Сохрани:** Если намерение из Базы _отсутствует_ в свежем списке (сейчас не обсуждается), **оставь его без изменений** (статус и заголовок прежние). Не помечай его как `Устарело` просто потому, что его нет в новом списке.

4.  **Создание (Объединить и записать):**
    - **Логика:**
      - **Если Предыдущее Согласование существует И Merge=True:** Объедини «Черновик свежих намерений» с «Базой».
      - **Иначе:** Используй «Черновик свежих намерений» напрямую.
    - **Действие:** Замени `{{INTENTS_PLACEHOLDER}}` на **Финальный список**.
    - **Действие:** Замени `{{QUESTIONS_PLACEHOLDER}}`.
    - **Шаблон содержимого:**

````markdown
### {N}. {Краткий заголовок}

- **Категория:** {Логика / Архитектура / ...}
- **Контекст:** {Спецификация / Код / Тесты / ...}
- **Намерение:** {Подробное описание одного ключевого желания}
- **Подход автора:** {Изначальный подход}
- **Предложение проверяющего:** {Что было предложено}
- **Решение автора:** {Прямой ответ ИЛИ Эмодзи на комментарии ревьюера. Если нет: «Ожидает ответа»}
- **Реакция проверяющего:** {Последующий комментарий}
- **Обоснование:** {Почему}
- **Статус:** {Согласовано / Готово / Отклонено / Обсуждение / Уточнение / Отложено / Устарело} {Добавь ✅ если решено/закрыто}
- **Результат:** {Кратко: изменилось ли видение? достигнуто ли согласие?}

> [{Имя проверяющего}]({anchor}): «{Краткий пересказ}»
>
> [{Имя автора}]({anchor}): «{Краткий пересказ}»

_(Если есть diff_hunk, включи его сюда. Если НЕТ, полностью удали этот блок кода.)_

```{lang}
{Относящийся к делу код из diff_hunk. Включи ВСЕ важные части. Сжимай логику только при необходимости.}
```
````

    - **Разделение:** Добавь горизонтальную черту `---` перед _действительно новыми_ намерениями, если это уместно.

    **Требование к отчёту по фазе:**
    В своём отчёте о завершении фазы ты **ОБЯЗАН** чётко указать:
    - Количество **Новых** созданных намерений.
    - Количество **Существующих** обновлённых намерений.
    - Количество **Устаревших** сохранённых намерений.

#### 5.6. Фаза 6: Отчёт о покрытии

**Анализ:**

- **Проверка:** Прочитай **Объединённый список** намерений. Сравни его с комментариями из JSON.
- **Сопоставь каждый комментарий:** Убедись, что каждый индекс (например, `§1`) сопоставлен с намерением.

**Создание:**

- Замени `{{COVERAGE_PLACEHOLDER}}`.
- **КРИТИЧЕСКИ ВАЖНО:** Не создавай таблицу по частям. Ты должен сгенерировать **всю** таблицу за один вызов `search_replace` после загрузки всего контекста.
- **Шаблон содержимого:**

  Создай **ЕДИНУЮ** таблицу. Сгруппируй комментарии по Дате. Вставляй «Строку-заголовок» для каждой новой даты. Перед таблицей поставь заголовок секции:

  ```markdown
  ---

  ## Отчёт о покрытии

  | {Индекс}          | {Дата/Время}   | {Пользователь} | {Заголовок (Кратко)} | {Намерение} | {Реакция} |
  | ----------------- | -------------- | ------ | ----------------- | --------- | ---------- |
  |                   | **{Дата}**     |        |                   |           |            |
  | [{Idx}]({anchor}) | {дд.ММ ЧЧ:мм}  | {User} | {4-6 слов}       | #{N}      | {Эмодзи}   |
  | [{Idx}]({anchor}) | {дд.ММ ЧЧ:мм}  | {User} | └ {4-6 слов}     | #{M},#{N} | {Эмодзи}   |
  |                   | **{След.Дата}** |        |                   |           |            |
  | [{Idx}]({anchor}) | {дд.ММ ЧЧ:мм}  | {User} | {4-6 слов}       | -         | {Эмодзи}   |
  | [{Idx}]({anchor}) | {дд.ММ ЧЧ:мм}  | {User} | ├ {4-6 слов}     | #{N}      | {Эмодзи}   |
  | [{Idx}]({anchor}) | {дд.ММ ЧЧ:мм}  | {User} | └ {4-6 слов}     | #{N}      | {Эмодзи}   |
  | [{Idx}]({anchor}) | {дд.ММ ЧЧ:мм}  | {User} | {4-6 слов}       | -         | {Эмодзи}   |
  ```

  **Правила:**
  1.  Используй `index` (например, `§1`) из JSON.
  2.  **Исчерпывающе:** У каждого комментария должна быть своя строка. Не объединяй их в диапазоны (например, `§1-§5`).
  3.  **Визуализация:** Используй `├` и `└`, чтобы показать ответы в ветке.
  4.  **Индикатор решения:** Если у ветки стоит `is_resolved: true`, ты **ОБЯЗАН** добавить `✅ ` в начало **Заголовка (Кратко)** (например, `✅ Исправить опечатку...`).
  5.  **Приоритет реакций (Иерархия):**
      - **Уровень 1 (Явные):** Если есть реакции на GitHub (например, `+1`, `heart`, `rocket`) -> используй этот эмодзи.
      - **Уровень 2 (Подразумеваемые):** Если реакций нет, определи по тексту:
        - `❓` (Вопрос без ответа)
        - `⚠️` (Предупреждение/Риск)
        - `🚧` (В работе/Ожидает)
        - `🗑️` (Удалено/Отклонено)
        - `💡` (Идея/Предложение)
        - `🤝` (Текст о согласии)
        - `🗣️` (Обычный ответ)
        - `✅` (Подтверждение о выполнении)
        - `👍` (Одобрение)
  6.  **Сортировка:** **Строго по Индексу (§1, §2...).** Входные данные уже сгруппированы по веткам и отсортированы. Ты ДОЛЖЕН сохранить этот порядок.
  7.  **Группировка:** Вставляй заголовок с датой `| | **{Дата}** | | | | |` ТОЛЬКО тогда, когда меняется **Дата начала ветки**. Не разрывай одну ветку на разные даты, даже если ответы были в разные дни. Вся ветка должна быть под датой своего первого сообщения.

#### 5.7. Фаза 7: Оценка от ИИ

**Создание:**

- Замени `{{OPINION_PLACEHOLDER}}`.
- **Требования к содержанию:**
  1.  **Здоровье сотрудничества:** Оцени тон общения, конструктивность отзывов и общую согласованность.
  2.  **Качество процесса:** Было ли ревью детальным? Были ли инструкции понятными?
  3.  **Анализ рисков:** Найди обсуждения, где есть путаница, высокая сложность или потенциальный технический долг.
  4.  **Ключевые блокеры:** Перечисли 1-2 главные нерешённые проблемы, которые мешают слиянию (если есть).
  5.  **Урок:** Какой полезный пример или анти-пример здесь проявился? (Поучительный момент).
  6.  **Дополнительные наблюдения (необязательно):** Добавь до 10 пунктов, если у тебя есть уникальные, ценные идеи. **ПРОПУСТИ**, если нечего добавить.

#### 5.8. Фаза 8: Проверка и очистка

1.  **Глубокая перепроверка:** Убедись, что ничего не упущено.
2.  **Исправление ссылок (Стратегия экономии токенов):**
    - **Контекст:** Во время генерации мы намеренно использовали короткие ID комментариев (например, `12345678`) в ссылках (например, `[Индекс](12345678)`) вместо полных URL, чтобы сэкономить место. Теперь нужно «наполнить» их, чтобы они стали кликабельными.
    - **Инструкция:** Замени `(ID)` на `(https://github.com/...#issuecomment-ID)`.
    - **Действие:** Выполни следующую команду `sed` (используя временный файл для совместимости), чтобы массово заменить все ID комментариев.
    - **Команда:**
      ```bash
      sed -E 's~]\((discussion_r[0-9]+|issuecomment-[0-9]+|pullrequestreview-[0-9]+)\)~](https://github.com/{OWNER}/{REPO}/pull/{PR_NUMBER}#\1)~g' {OUTPUT_DIR}/{FILENAME}.md > {OUTPUT_DIR}/{FILENAME}.tmp && mv {OUTPUT_DIR}/{FILENAME}.tmp {OUTPUT_DIR}/{FILENAME}.md
      ```
3.  **Прочитай файл:** Убедись, что все заглушки (`placeholder`) исчезли.
4.  **Очистка:** Удали файл `{OUTPUT_DIR}/{FILENAME}.ndjson`.

#### 5.9. Фаза 9: Автопостинг (Необязательно)

1.  **Проверь настройки:**
    - Если `auto_post` равно **false**: Отметь фазу как выполненную и **ВЫХОДИ**.

2.  **Подготовь содержимое:**
    - Прочитай итоговое содержимое файла `{OUTPUT_DIR}/{FILENAME}.md`.

3.  **Опубликуй или обнови:**
    - **Проверь:** Посмотри на свой отчёт по Фазе 5. Ты нашёл ID комментария Согласования?
    - **Обновить (PATCH):** Если ID существует (замени `{ID}` на реальный номер):
      ```bash
      jq -n --rawfile content {OUTPUT_DIR}/{FILENAME}.md '{body: $content}' | \
      gh api -X PATCH "repos/{OWNER}/{REPO}/issues/comments/{ID}" --input -
      ```
    - **Создать (POST):** Если ID не был найден:
      ```bash
      jq -n --rawfile content {OUTPUT_DIR}/{FILENAME}.md '{body: $content}' | \
      gh api -X POST "repos/{OWNER}/{REPO}/issues/{PR_NUMBER}/comments" --input -
      ```

4.  **Финальная настройка (По условию):**
    - Если `include_instructions` равно **true**:
      - Замени `{{WARNING_PLACEHOLDER}}` на:
        ```markdown
        > [!WARNING] АВТОГЕНЕРАЦИЯ: НЕ РЕДАКТИРОВАТЬ, НЕ КОММИТИТЬ
        > Этот документ — **инструмент синхронизации**. Он служит для:
        >
        > 1. **Проверки:** Убеждается, что 100% комментариев услышаны.
        > 2. **Синтеза:** Превращает обсуждение в план (Консенсус).
        > 3. **Запроса:** Даёт инструкции для обновления Предложения.
        >
        > Если документ некорректен, **не редактируйте его вручную**. Добавьте комментарии в PR и **сгенерируйте заново**, чтобы замкнуть цикл обратной связи.
        >
        > **НЕ ДОБАВЛЯЙТЕ ЭТОТ ФАЙЛ В GIT.**
        >
        > - Подробнее: [22: Alignment](https://idealic.academy/raw/en/company/22_document_alignment.md)
        > - В Git разрешены только: [Предложение](https://idealic.academy/raw/en/company/21_document_proposal.md) и [Спецификация](https://idealic.academy/raw/en/company/20_document_spec.md).
        ```
      - Замени `{{INSTRUCTIONS_PLACEHOLDER}}` на:

        ```markdown
        ## Инструкции для Агента (Следующий шаг)

        > [!IMPORTANT] Загрузка контекста
        > Прежде чем вносить изменения в Предложение или Спецификацию, ты **ОБЯЗАН** загрузить следующие файлы, чтобы полностью соответствовать стандартам компании:
        >
        > 1. `curl https://idealic.academy/raw/en/company/02_process.md` (Процесс)
        > 2. `curl https://idealic.academy/raw/en/company/50_prompt_truth.md` (Истина)
        > 3. `curl https://idealic.academy/raw/en/company/20_document_spec.md` (Спецификация)
        > 4. `curl https://idealic.academy/raw/en/company/21_document_proposal.md` (Предложение)
        > 5. `curl https://idealic.academy/raw/en/company/22_document_alignment.md` (Согласование)
        ```

    - Если `include_instructions` равно **false**:
      - Удали `{{WARNING_PLACEHOLDER}}` и `{{INSTRUCTIONS_PLACEHOLDER}}` (замени на пустую строку).

5.  **Очистка:** Не требуется (временные файлы не использовались).

### 6. Финальный вывод

Выведи итог проделанной работы, расскажи об изменениях и поделись своими мыслями.
