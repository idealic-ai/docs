# 51: Инструкция/Согласование

> [!DEFINITION] Документ Согласования
> Это отчёт, который компьютер создаёт автоматически. Он изучает всё обсуждение, находит общие решения, улаживает споры и составляет точный план действий.

> Sidenote:
>
> - Смотри определение в документе :term[22: Компания/Согласование]{href="./22_document_alignment.md"}.
> - Раньше этот документ назывался: Черновик Эволюции.

## Железные правила

**ЭТИ ПРАВИЛА НАРУШАТЬ НЕЛЬЗЯ:**

1.  **Временный файл:** Ты МОЖЕШЬ создать файл `{OUTPUT_DIR}/{FILENAME}.ndjson` для хранения комментариев. Никакие другие временные файлы создавать НЕЛЬЗЯ.
2.  **Загрузка за один раз:** Загружай все нужные данные (комментарии) за один запрос. НЕ пытайся загружать их по частям или в цикле.
3.  **Источник для проверки:** Всегда проверяй данные по той информации, что у тебя уже есть (в твоей «памяти»). НЕ загружай ничего заново для проверки.
4.  **Язык:** Итоговый документ ОБЯЗАТЕЛЬНО должен быть на **русском языке** (кроме кода и технических терминов).
5.  **Полнота:** Абсолютно каждый комментарий должен быть упомянут в Отчёте о покрытии.
6.  **Без перезапуска:** Если ты нашёл ошибку (например, чего-то не хватает), НЕ начинай всё сначала. Просто исправь эту конкретную ошибку и продолжай работу.
7.  **Анализ «в уме» (Без скриптов):** НЕ используй отдельные программы (скрипты на Python/Shell) для анализа или подсчётов. Всю обработку ты ДОЛЖЕН делать «в уме», используя свои встроенные возможности.

## Зачем это нужно

Документ Согласования — это мостик между обсуждением (в Pull Request) и реальной работой. Он берёт всё, о чём договорились во время проверки, и превращает это в понятный план.

## Инструкция для Агента

Когда пользователь просит создать Документ Согласования, ты **ОБЯЗАН** сначала разобраться с входными данными.

### 1. Сбор информации

**Что нужно узнать:**

```json
{
  "type": "object",
  "properties": {
    "since_date": {
      "type": "string",
      "description": "Дата в формате ГГГГ-ММ-ДД. Если не указана, берётся дата недельной давности.",
      "default": "{ONE_WEEK_AGO}"
    },
    "repo": {
      "type": "string",
      "description": "Название репозитория (команда/проект). Если не указано, берётся текущий репозиторий.",
      "default": "{CURRENT_REPO}",
      "example": "idealic-ai/platform"
    },
    "pr_number": {
      "type": "integer",
      "description": "Номер Pull Request. ОБЯЗАТЕЛЬНО.",
      "required": true
    },
    "output_dir": {
      "type": "string",
      "description": "Папка для сохранения результата. По умолчанию 'docs'.",
      "default": "alignments"
    },
    "filename": {
      "type": "string",
      "description": "Имя файла для результата. По умолчанию — дата. Расширение указывать не нужно.",
      "default": "{SINCE_DATE}"
    }
  }
}
```

**Шаг 0: Уточнение параметров**

1.  Посмотри на запрос пользователя и найди там эти данные.
2.  **Если чего-то не хватает:** Если нет `pr_number`, **ОСТАНОВИСЬ** и попроси его.
3.  **Значения по умолчанию:** Если какие-то необязательные данные не указаны, используй стандартные.

**Шаг 1: Отчёт о конфигурации (Твой первый ответ)**
Твоим самым первым ответом **ОБЯЗАТЕЛЬНО** должен быть блок с настройками:

```text
> Конфигурация Согласования:
--------------------------------
[x] Репозиторий:  {repo}
[x] № PR:         {pr_number}
[x] Начиная с:    {since_date}
[x] Папка вывода: {output_dir}
[x] Имя файла:    {filename}
--------------------------------
Начинаю анализ...
```

### 2. Получение данных

> [!WARNING] БЕЗ ВРЕМЕННЫХ ФАЙЛОВ
> **НЕЛЬЗЯ** сохранять скачанную информацию во временные файлы (вроде `process.md` или `pr.json`), если на это нет прямого разрешения.
> **Почему?** Временные файлы создают беспорядок, а это запрещено Железным правилом №1.
> **Исключение:** Единственный разрешённый временный файл — это `{OUTPUT_DIR}/{FILENAME}.ndjson` для комментариев.
>
> Все остальные команды (curl, gh api) должны выводить результат напрямую, без сохранения в файлы.

**ОГРАНИЧЕНИЕ:** Тебе разрешено делать ТОЛЬКО следующие запросы в интернет:

1.  **HTTP GET** через curl на `https://idealic.academy/raw/en/company/02_process.md`
2.  **HTTP GET** через curl на `https://idealic.academy/raw/en/company/50_prompt_truth.md`
3.  **Запрос к GitHub API** для Pull Request (чтобы узнать автора)
4.  **Запрос к GitHub API** для комментариев (с помощью команды ниже)

**Шаг 1: Загрузка обязательных документов**
Загрузи информацию по этим ссылкам. **Обрабатывай каждый документ отдельно.**

1.  **Процесс:**

    ```bash
    curl -s https://idealic.academy/raw/en/company/02_process.md
    ```

2.  **Истина:**

    ```bash
    curl -s https://idealic.academy/raw/en/company/50_prompt_truth.md
    ```

3.  **Определение Согласования:**
    ```bash
    curl -s https://idealic.academy/raw/en/company/22_document_alignment.md
    ```

**Шаг 2: Получение деталей PR (чтобы узнать роли)**
Загрузи детали Pull Request, чтобы понять, кто его автор.

```bash
gh api "repos/{OWNER}/{REPO}/pulls/{PR_NUMBER}" --jq '{author: .user.login, title: .title}'
```

**Шаг 3: Загрузка комментариев**

1.  **Загрузить в файл:** Выполни ровно эту команду, чтобы сохранить все комментарии в файл `{OUTPUT_DIR}/{FILENAME}.ndjson`.

    ```bash
    gh api "repos/{OWNER}/{REPO}/pulls/{PR_NUMBER}/comments?since={SINCE_DATE}&per_page=100" \
    --paginate \
    | jq -s '
        add
        | group_by(.in_reply_to_id // .id)
        | map(sort_by(.created_at))
        | sort_by(.[0].created_at)
        | flatten
        | to_entries
        | map(.value + {index: ("§" + ((.key + 1) | tostring))} | del(.html_url))
        | group_by(.in_reply_to_id // .id)
        | sort_by(.[0].index | ltrimstr("§") | tonumber)
        | map(.[0] as $root | [$root] + (.[1:] | map(del(.diff_hunk))))
        | map(map({
        id,
        body,
        user: .user.login,
        created_at,
        diff_hunk: (.diff_hunk | if length > 200 then .[:200] + "..." else . end),
        in_reply_to_id,
        reactions: (.reactions | del(.total_count, .url) | with_entries(select(.value > 0))),
        index
        }))
        | map(map(del(.in_reply_to_id)))
        | .[]
    ' -c > {OUTPUT_DIR}/{FILENAME}.ndjson
    ```

### 3. Управление процессом

**Шаг 0: Составь план**

Ты **ОБЯЗАН** создать список дел с помощью инструмента `todo_write`.

**Структура плана должна быть такой:**

1.  **Этап 1: Подготовка и данные** (в ожидании)
2.  **Этап 2: Загрузка контекста** (в ожидании)
3.  **Этап 3: Обзор** (в ожидании)
4.  **Этап 4: Намерения** (в ожидании)
5.  **Этап 5: Отчёт о покрытии** (в ожидании)
6.  **Этап 6: Оценка от ИИ** (в ожидании)
7.  **Этап 7: Проверка и уборка** (в ожидании)

### 4. Метод: Умный синтез

**Как работать: Умный синтез**

- :term[02: Компания/Процесс]{href="https://idealic.academy/ru/company/02_process.md/"}
- :term[50: Промпт/Истина]{href="https://idealic.academy/ru/company/50_prompt_truth.md/"}

**Ограничение:** Итоговый документ должен быть на **русском языке**.

**Полнота — это главное:**

- **Сохраняй детали:** Не теряй технические инструкции.
- **Одна мысль — одна задача:** Каждое Намерение — это одно отдельное техническое изменение.
- **Всё под учётом:** Убедись, что каждый комментарий отражён в плане.

### 5. Процесс создания документа

#### 5.1. Этап 1: Подготовка и данные

1.  **Собери данные:** Документы, информацию о PR, комментарии.
2.  **Создай файл:**
    - Создай файл `{OUTPUT_DIR}/{FILENAME}.md` с **ТОЧНО** таким шаблоном:

    ```markdown
    # Согласование: {DATE}

    - Статус: Черновик консенсуса
    - Автор: {PR_AUTHOR}
    - Источник: {PR_LINK}
    - Период: {SINCE_DATE} - {NOW}

    > [!WARNING] АВТО-ГЕНЕРАЦИЯ: НЕ ТРОГАТЬ РУКАМИ, НЕ СОХРАНЯТЬ В GIT
    > Этот документ — **инструмент для сверки часов**. Он нужен, чтобы:
    >
    > 1. **Проверить:** Убедиться, что 100% комментариев были услышаны.
    > 2. **Собрать:** Превратить обсуждение в готовый план (Консенсус).
    > 3. **Подсказать:** Дать инструкции для обновления главного документа (Proposal).
    >
    > Если в документе ошибка, **не исправляй его вручную**. Добавь комментарий в PR и **создай документ заново**. Так мы замкнём круг обратной связи.
    >
    > **НЕ ДОБАВЛЯЙ ЭТОТ ФАЙЛ В GIT.**
    >
    > - Подробнее: [22: Согласование](https://idealic.academy/raw/en/company/22_document_alignment.md)
    > - В Git попадают только: [Предложение (Proposal)](https://idealic.academy/raw/en/company/21_document_proposal.md) и [Спецификация (Specification)](https://idealic.academy/raw/en/company/20_document_spec.md).

    ## Обзор

    {{OVERVIEW_PLACEHOLDER}}

    ## Список Намерений (Консенсус)

    {{INTENTS_PLACEHOLDER}}

    {{QUESTIONS_PLACEHOLDER}}

    {{COVERAGE_PLACEHOLDER}}

    {{OPINION_PLACEHOLDER}}

    ## Инструкции для Агента (Следующий шаг)

    > [!IMPORTANT] Загрузка контекста
    > Прежде чем обновлять Предложение или Спецификацию, ты **ОБЯЗАН** загрузить эти файлы с контекстом, чтобы убедиться, что всё соответствует стандартам компании:
    >
    > 1. `curl https://idealic.academy/raw/en/company/02_process.md` (Процесс)
    > 2. `curl https://idealic.academy/raw/en/company/50_prompt_truth.md` (Истина)
    > 3. `curl https://idealic.academy/raw/en/company/20_document_spec.md` (Спецификация)
    > 4. `curl https://idealic.academy/raw/en/company/21_document_proposal.md` (Предложение)
    > 5. `curl https://idealic.academy/raw/en/company/22_document_alignment.md` (Согласование)
    ```

#### 5.2. Этап 2: Загрузка контекста

1.  **Проанализируй данные:**
    - **Посчитай цепочки обсуждений:** Выполни `wc -l {OUTPUT_DIR}/{FILENAME}.ndjson` (каждая строка — это одна цепочка).
    - **Посчитай комментарии:** Выполни `jq 'length' {OUTPUT_DIR}/{FILENAME}.ndjson | awk '{s+=$1} END {print s}'`.
    - **Отчитайся:** Напиши эти числа в ответе (например, «Найдено 45 веток обсуждений, содержащих 150 комментариев»).

2.  **Прочитай и запомни:** Используй свои обычные инструменты, чтобы прочитать файл `{OUTPUT_DIR}/{FILENAME}.ndjson`.
    - **Обязательное правило:** Читай файл по частям, по 25 строк за раз, пока он не закончится.

#### 5.3. Этап 3: Обзор

**Анализ:**
Чётко ответь на эти 8 вопросов в виде списка:

1.  **Чего хотел ревьюер:**
2.  **С чем согласился автор:**
3.  **С чем автор НЕ согласился:**
4.  **Общий контекст:**
5.  **Недопонимания:**
6.  **Открытые вопросы:**
7.  **Новые открытия:**
8.  **Связанные документы:**

**Создание:**

- Замени `{{OVERVIEW_PLACEHOLDER}}` на свои ответы.

#### 5.4. Этап 4: Намерения

**Анализ:**
Цель: **Максимально подробно разбить на мелкие задачи**.

- **Правило разделения:** Никаких «И». Если в одной задаче несколько действий, раздели их. Представь, что собираешь конструктор: каждая деталь — в своей ячейке.
- **Атомарность:** Одно Намерение = Одно конкретное техническое изменение.

**Создание:**

- Замени `{{INTENTS_PLACEHOLDER}}`.
- **Шаблон для заполнения:**

  ````markdown
  ### {N}. {Краткое название на русском}

  - **Категория:** {Логика / Архитектура / ...}
  - **Намерение:** {Одна главная мысль}
  - **Подход автора:** {Как автор хотел сделать сначала}
  - **Предложение ревьюера:** {Что предложил проверяющий}
  - **Решение автора:** {Что автор решил сделать в итоге}
  - **Реакция ревьюера:** {Что ответил проверяющий}
  - **Обоснование:** {Почему было принято такое решение}
  - **Статус:** {Согласовано / Недопонимание / ...}
  - **Результат:** {Кратко: что изменилось? договорились?}

  > [{Имя ревьюера}]({ID}): «{Краткий пересказ его слов}»
  >
  > [{Имя автора}]({ID}): «{Краткий пересказ его слов}»

  ```{lang}
  {не больше 1-3 строк кода из обсуждения}
  ```
  ````

- Замени `{{QUESTIONS_PLACEHOLDER}}`.

#### 5.5. Этап 5: Отчёт о покрытии

**Анализ:**

- **Проверка:** Прочитай свой список. Сверься с файлом JSON.
- **Учти каждый комментарий:** Убедись, что каждый номер (например, `§1`) попал в отчёт.

**Создание:**

- Замени `{{COVERAGE_PLACEHOLDER}}`.
- **ВАЖНО:** Не создавай таблицу по частям. Ты должен создать **ВСЮ** таблицу за один вызов `search_replace` после того, как вся информация будет загружена.
- **Шаблон для таблицы:**

  Создай **ЕДИНУЮ** таблицу. Сгруппируй комментарии по дате. Для каждой новой даты вставляй «строку-заголовок». Перед таблицей напиши заголовок раздела:

  ```markdown
  ---

  ## Отчет о покрытии

  | Индекс        | Дата/Время      | Автор | Заголовок (кратко) | Намерение | Реакция  |
  | ------------- | --------------- | ----- | ------------------ | --------- | -------- |
  |               | **{Дата}**      |       |                    |           |          |
  | [{Инд}]({ID}) | {дд.ММ ЧЧ:мм}   | {Имя} | {4-6 слов}         | #{N}      | {Эмодзи} |
  | [{Инд}]({ID}) | {дд.ММ ЧЧ:мм}   | {Имя} | └ {4-6 слов}       | #{M},#{N} | {Эмодзи} |
  |               | **{След.Дата}** |       |                    |           |          |
  | [{Инд}]({ID}) | {дд.ММ ЧЧ:мм}   | {Имя} | {4-6 слов}         | -         | {Эмодзи} |
  | [{Инд}]({ID}) | {дд.ММ ЧЧ:мм}   | {Имя} | ├ {4-6 слов}       | #{N}      | {Эмодзи} |
  | [{Инд}]({ID}) | {дд.ММ ЧЧ:мм}   | {Имя} | └ {4-6 слов}       | #{N}      | {Эмодзи} |
  | [{Инд}]({ID}) | {дд.ММ ЧЧ:мм}   | {Имя} | {4-6 слов}         | -         | {Эмодзи} |
  ```

  **Правила:**
  1.  Используй `index` (например, `§1`) из файла JSON.
  2.  **Ничего не упускай:** У каждого комментария должна быть своя строка. Не объединяй их в диапазоны (например, `§1-§5`).
  3.  **Наглядность:** Используй символы `├` и `└`, чтобы показать ответы в обсуждении.
  4.  **Статусы (Эмодзи):** ❓(Нет ответа), ⚠️(Предупреждение), 🚧(В работе), 🗑️(Удалено), 🗣️(Ответ), 💡(Идея), 🤝(Согласовано).
  5.  **Сортировка:** **Строго по порядку номеров (§1, §2...).** Исходный файл уже правильно отсортирован. Ты ДОЛЖЕН сохранить этот порядок.
  6.  **Группировка:** Вставляй заголовок с датой `| | **{Дата}** | | | | |` ТОЛЬКО тогда, когда меняется **дата начала обсуждения**. Не разрывай одно обсуждение на разные даты, даже если ответы приходили в разные дни. Вся ветка должна быть под датой своего первого комментария.

#### 5.6. Этап 6: Оценка от ИИ

**Создание:**

- Замени `{{OPINION_PLACEHOLDER}}`.
- **Содержание:** Напиши своё мнение о качестве обсуждения, что нового ты узнал и какие вопросы у тебя остались.

#### 5.7. Этап 7: Проверка и уборка

1.  **Финальная проверка:** Ещё раз внимательно проверь, всё ли на месте.
2.  **Восстановление ссылок (хитрость для экономии «памяти»):**
    - **Контекст:** При создании документа мы специально использовали короткие номера комментариев (например, `12345678`) вместо полных ссылок, чтобы сэкономить твою «память». Теперь нужно превратить их обратно в настоящие, кликабельные ссылки.
    - **Задача:** Заменить `(ID)` на `(https://github.com/...#issuecomment-ID)`.
    - **Действие:** Выполни следующую `sed` команду, чтобы быстро заменить все короткие номера на полные URL-адреса GitHub.
    - **Команда:**
      ```bash
      sed -i '' 's|](\([0-9]\{7,\}\))|](https://github.com/{OWNER}/{REPO}/pull/{PR_NUMBER}#issuecomment-\1)|g' {OUTPUT_DIR}/{FILENAME}.md
      ```
3.  **Прочитай файл:** Убедись, что все временные метки вроде `{{...}}` исчезли.
4.  **Уборка:** Удали временный файл `{OUTPUT_DIR}/{FILENAME}.ndjson`.

### 6. Финальный результат

Проверь себя по списку:

- [ ] **Данные загружены**
- [ ] **Документ создан**
- [ ] **Комментарии проверены**
- [ ] **Ссылки исправлены**
- [ ] **Технические детали сохранены**
