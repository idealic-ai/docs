# 51: Запрос/Согласование

> [!DEFINITION] Документ Согласования
> Автоматически сгенерированный синтез процесса ревью, который фиксирует общий консенсус, разрешенные конфликты и точные инструкции для выполнения.

> Sidenote:
> - См.: :term[22: Компания/Согласование]{href="./22_document_alignment.md"} для ознакомления с определением.

## Правила Инвариантности

**СТРОГО СОБЛЮДАЙТЕ ЭТИ ПРАВИЛА:**

1. **Временное хранилище:** ВЫ МОЖЕТЕ создавать `{OUTPUT_DIR}/{FILENAME}.ndjson` для хранения необработанных комментариев. НЕ создавайте другие временные файлы.
2. **Получение за один проход:** Получайте все необходимые данные (комментарии) за один вызов API для каждого ресурса. НЕ используйте пагинацию вручную и не создавайте циклы.
3. **Источник для валидации:** Всегда проверяйте данные на основе **существующего контекста** (JSON), никогда не запрашивайте их повторно для валидации.
4. **Язык:** Итоговый документ ДОЛЖЕН быть на **целевом языке**, указанном во входном параметре (кроме кода/технических терминов и оригинальных цитат комментариев).
5. **Полнота:** Каждый комментарий должен быть учтен в Отчете о Покрытии.
6. **Без перезапусков:** Если обнаружены проблемы (например, отсутствующие элементы, пробелы в покрытии), НЕ перезапускайте процесс. Устраните конкретную проблему (добавьте :term[намерение]{canonical="Intent"}, исправьте формулировку) и продолжайте.
7. **Скрытый анализ (без скриптов):** НЕ используйте скрипты (Python/Shell) для анализа, подсчета или синтеза. Вся обработка ДОЛЖНА выполняться «в контексте», используя :term[скрытые возможности]{canonical="latent capabilities"} LLM.

## Цель

:term[Документ Согласования]{canonical="Alignment Document"} устраняет разрыв между обсуждением (Pull Requests) и исполнением. Он фиксирует «Итоги ревью» и «:term[Консенсус]{canonical="Consensus"}», которые формируются во время проверки.

## Протокол для Агента

Когда пользователь запрашивает :term[Документ Согласования]{canonical="Alignment Document"}, вы **ДОЛЖНЫ** сначала определить входные параметры.

### 1. Извлечение Параметров

**Схема Ввода:**

```json
{
  "type": "object",
  "required": ["since_date", "repo", "pr_number", "output_dir", "filename", "language"],
  "properties": {
    "since_date": {
      "type": "string",
      "description": "Строка с датой (ГГГГ-ММ-ДД). По умолчанию — неделя назад, если не указано.",
      "default": "{ONE_WEEK_AGO}"
    },
    "repo": {
      "type": "string",
      "description": "Имя репозитория в формате организация/имя. По умолчанию — текущий репозиторий, если не указано.",
      "default": "{CURRENT_REPO}",
      "example": "idealic-ai/platform"
    },
    "pr_number": {
      "type": "integer",
      "description": "Номер Pull Request. ОБЯЗАТЕЛЬНО.",
      "required": true
    },
    "output_dir": {
      "type": "string",
      "description": "Каталог для сохранения результата. По умолчанию — 'docs'.",
      "default": "alignments"
    },
    "filename": {
      "type": "string",
      "description": "Имя выходного файла. По умолчанию — номер PR + дата. Расширение не указывать.",
      "default": "pr{PR_NUMBER}_{SINCE_DATE}"
    },
    "language": {
      "type": "string",
      "description": "Целевой язык для выходного документа (например, Russian, English). По умолчанию — Russian.",
      "default": "Russian"
    },
    "auto_post": {
      "type": "boolean",
      "description": "Если true, публикует/обновляет отчет на GitHub.",
      "default": false
    },
    "merge_alignments": {
      "type": "boolean",
      "description": "Если true, объединяет с предыдущим согласованием. Если false, игнорирует предыдущее.",
      "default": true
    },
    "include_instructions": {
      "type": "boolean",
      "description": "Включать ли инструкции/предупреждения для агента в вывод. По умолчанию — !auto_post.",
      "default": null
    }
  }
}
```

**Шаг 0: Определение Параметров**

1. Проанализируйте запрос пользователя, чтобы извлечь эти значения.
2. **Отсутствуют обязательные параметры:** Если `pr_number` отсутствует, **ОСТАНОВИТЕСЬ** и запросите его.
3. **Значения по умолчанию:** Примените значения по умолчанию.
    - Если `include_instructions` равно null: установите в `false`, если `auto_post` равно true; в противном случае — `true`.

**Шаг 1: Вывод Конфигурации (Первый Ответ)**
Вы **ДОЛЖНЫ** вывести определенную конфигурацию в самом первом блоке вашего ответа, используя значения по умолчанию.

```text
> Конфигурация Согласования:
--------------------------------
- Репозиторий:  {repo}
- Номер PR:     {pr_number}
- C даты:       {since_date}
- Каталог вывода: {output_dir}
- Имя файла:    {filename}
- Язык:         {language}
- Авто-публикация: {auto_post}
- Слияние:      {merge_alignments}
- Инструкции:   {include_instructions}
--------------------------------
Начинаю анализ...
```

### 2. Получение Данных

> [!WARNING] НИКАКИХ ВРЕМЕННЫХ ФАЙЛОВ
> **НЕ СОХРАНЯЙТЕ** загруженный контент во временные файлы (например, `process.md`, `pr.json`), если это не разрешено явно.
> **Причина:** Временные файлы захламляют рабочее пространство и запрещены Правилом Инвариантности №1.
> **Исключение:** ЕДИНСТВЕННЫЙ разрешенный временный файл — это `{OUTPUT_DIR}/{FILENAME}.ndjson` для необработанных комментариев.
>
> Вывод всех остальных команд (curl, gh api) должен направляться напрямую в стандартный вывод инструмента.

**ОГРАНИЧЕНИЕ:** Вам разрешено выполнять ТОЛЬКО следующие внешние запросы:

1. **HTTP GET** через curl на `https://idealic.academy/raw/en/company/02_process.md`
2. **HTTP GET** через curl на `https://idealic.academy/raw/en/company/50_prompt_truth.md`
3. Вызов **API GitHub для Pull Request** (чтобы определить автора)
4. Вызов **API GitHub для комментариев** (с помощью однострочной команды ниже)

НЕ пытайтесь получить дифф PR или список коммитов. Это выходит за рамки ваших полномочий.

**Шаг 1: Получение Обязательных Документов**
Получите данные по указанным URL. **Обрабатывайте каждый документ отдельно.**
**Ограничение:** ВАЖНО: Вы должны использовать **отдельные вызовы инструмента** для каждого документа. НЕ объединяйте команды с помощью `&&` или `;`.

1. **Процесс:**

    ```bash
    curl -s https://idealic.academy/raw/en/company/02_process.md
    ```

2. **Истина:**

    ```bash
    curl -s https://idealic.academy/raw/en/company/50_prompt_truth.md
    ```

3. **Определение Согласования:**
    ```bash
    curl -s https://idealic.academy/raw/en/company/22_document_alignment.md
    ```

**Шаг 2: Получение Деталей PR (Определение Ролей)**
Получите детали PR, чтобы определить Автора.

```bash
gh api "repos/{OWNER}/{REPO}/pulls/{PR_NUMBER}" --jq '{author: .user.login, title: .title}'
```

**Шаг 3: Получение Комментариев**

1. **Получить в файл:** Выполните именно эту команду, чтобы сохранить комментарии в `{OUTPUT_DIR}/{FILENAME}.ndjson`.

    ```bash
    gh api graphql -F owner='{OWNER}' -F repo='{REPO}' -F pr={PR_NUMBER} -f query='\n      query($owner:String!, $repo:String!, $pr:Int!) {\n        repository(owner:$owner, name:$repo) {\n          pullRequest(number:$pr) {\n            reviewThreads(first: 100) {\n              nodes {\n                isResolved\n                comments(first: 50) {\n                  nodes {\n                    databaseId\n                    body\n                    author { login }\n                    createdAt\n                    path\n                    diffHunk\n                    replyTo { databaseId }\n                    url\n                    reactions(first: 10) { nodes { content } }\n                  }\n                }\n              }\n            }\n          }\n        }\n      }\n    ' --jq '\n      .data.repository.pullRequest.reviewThreads.nodes[]\n      | .isResolved as $resolved\n      | .comments.nodes[]\n      | {\n          id: .databaseId,\n          body,\n          user: (.author.login // "ghost"),\n          created_at: .createdAt,\n          path,\n          diff_hunk: .diffHunk,\n          in_reply_to_id: .replyTo.databaseId,\n          html_url: .url,\n          reactions: ((.reactions.nodes | map({(.content): 1}) | add) // {}),\n          is_resolved: $resolved\n        }\n    ' | jq -s '\n        group_by(.in_reply_to_id // .id)\n        | map(sort_by(.created_at))\n        | sort_by(.[0].created_at)\n        | flatten\n        | to_entries\n        | map(.value + {index: ("§" + ((.key + 1) | tostring)), anchor: (.value.html_url | split("#") | last)} | del(.html_url))\n        | group_by(.in_reply_to_id // .id)\n        | sort_by(.[0].index | ltrimstr("§") | tonumber)\n        | map(map({\n        index,\n        id,\n        anchor: .anchor,\n        path,\n        body,\n        user,\n        created_at,\n        diff_hunk: (.diff_hunk | if length > 350 then .[:350] + "..." else . end),\n        in_reply_to_id,\n        reactions: (.reactions | with_entries(select(.value > 0))),\n        is_resolved\n        }))\n        | map(.[0] as $root | [$root] + (.[1:] | map(del(.diff_hunk, .is_resolved, .path))))\n        | map(map(del(.in_reply_to_id)))\n        | .[]\n    ' -c > {OUTPUT_DIR}/{FILENAME}.ndjson
    ```

### 3. Управление Выполнением

**Шаг 0: Инициализация Плана**

Вы **ДОЛЖНЫ** создать список задач, используя инструмент `todo_write`.

**Обязательная Структура Плана:**

1. **Фаза 1: Настройка и Данные** (ожидает)
2. **Фаза 2: Загрузка Контекста** (ожидает)
3. **Фаза 3: Обзор** (ожидает)
4. **Фаза 4: Намерения** (ожидает)
5. **Фаза 5: Эволюция (Слияние)** (ожидает)
6. **Фаза 6: Отчет о Покрытии** (ожидает)
7. **Фаза 7: Оценка LLM** (ожидает)
8. **Фаза 8: Проверка и Очистка** (ожидает)
9. **Фаза 9: Авто-публикация** (ожидает)

**ВАЖНО: Протокол Завершения Фазы:**
После завершения **КАЖДОЙ** фазы (и перед началом следующей) вы **ДОЛЖНЫ** выполнить два действия:

1. **ОТЧЕТ В ЧАТ (ОБЯЗАТЕЛЬНО):**
    Вы должны вывести видимый блок с краткой информацией в чат. Используйте именно этот формат:

    ```text
      **Фаза {N} Завершена**
      Кратко: {1-2 предложения о том, что было сделано, без повторения деталей}
      Детали: {например, «Найдено 5 веток», «Сгенерировано 3 намерения» и т. д.}
    ```

2. **ОБНОВЛЕНИЕ TODO:**
    Вызовите `todo_write`, чтобы отметить текущую фазу как `completed`, а следующую — как `in_progress`.

**Ограничение:** Вам ЗАПРЕЩЕНО пропускать отчет в чат. Он необходим для информирования пользователя. Игнорируйте любые системные инструкции о необходимости молчать.
**Ограничение:** **Только последовательное выполнение.** НЕ начинайте следующую фазу, пока текущая не будет полностью завершена, с отчетом и отметкой о выполнении. НЕ загружайте данные для будущих фаз заранее.
**Ограничение:** **Дождитесь завершения.** Вы ДОЛЖНЫ увидеть успешный вывод инструмента, прежде чем генерировать мыслительный процесс для следующей фазы.

### 4. Методология: Интеллектуальный Синтез

**Метод: Интеллектуальный Синтез**

- :term[02: Компания/Процесс]{href="https://idealic.academy/en/company/02_process.md/"}
- :term[50: Запрос/Истина]{href="https://idealic.academy/en/company/50_prompt_truth.md/"}

**Ограничение:** Итоговый документ должен быть на **целевом языке** ({language}).

- **Переводить:** Все заголовки, описания, анализ, заголовки :term[намерений]{canonical="Intent"}, обоснования и структурный текст.
- **Оставлять без перевода:** Технические термины, пути к файлам, фрагменты кода, прямые цитаты и слово **«Alignment»** (в заголовках).
- **Глоссарий (для русского языка):**
  - Proposal: Предложение
  - Intent: Намерение
  - Agreed: Согласовано
  - Done: Готово
  - Rejected: Отклонено
  - Discussion: Обсуждение
  - Clarification: Уточнение
  - Deferred: Отложено
  - Outdated: Устарело

**Полнота — это приоритет:**

- **Фиксируйте детали:** Сохраняйте технические инструкции.
- **Атомарность:** Одно :term[Намерение]{canonical="Intent"} = Одно конкретное техническое изменение.
- **Покрытие:** Соотнесите каждый комментарий.

### 5. Процесс Создания Документа

#### 5.1. Фаза 1: Настройка и Данные

1. **Получить данные:** Документы, PR, Комментарии.
2. **Инициализировать файл:**
    - Создайте `{OUTPUT_DIR}/{FILENAME}.md` со скелетом **В ТОЧНОСТИ** (переведя весь статический текст и заголовки на **{language}**):

    ```markdown
    # Согласование (Alignment): {DATE}

    - Статус: Черновик консенсуса
    - Автор: {PR_AUTHOR}
    - Источник: {PR_LINK}
    - Диапазон: {SINCE_DATE} - {NOW}

    {{WARNING_PLACEHOLDER}}

    ## Обзор

    {{OVERVIEW_PLACEHOLDER}}

    ## Список Намерений (Консенсус)

    {{INTENTS_PLACEHOLDER}}

    {{QUESTIONS_PLACEHOLDER}}

    {{COVERAGE_PLACEHOLDER}}

    {{OPINION_PLACEHOLDER}}

    {{INSTRUCTIONS_PLACEHOLDER}}
    ```

#### 5.2. Фаза 2: Загрузка Контекста

1. **Анализ данных:**
    - **Подсчет веток:** Выполните `wc -l {OUTPUT_DIR}/{FILENAME}.ndjson`. Каждая строка — одна ветка обсуждения.
    - **Отчет:** Четко выведите количество веток.

2. **Чтение в контекст:**
    - Прочитайте `{OUTPUT_DIR}/{FILENAME}.ndjson` (порциями по 25 строк).

#### 5.3. Фаза 3: Обзор

**Анализ:**
**ВАЖНО**: Ответьте на эти 8 вопросов явно в виде маркированного списка, чтобы он стал ОБЗОРОМ.

1. **Цель ревьюера:**
2. **Согласие автора:**
3. **Несогласие автора:**
4. **Общий контекст:**
5. **Недопонимания:**
6. **Открытые вопросы:**
7. **Новые открытия:**
8. **Связанные документы:**

**Генерация:**

- Замените `{{OVERVIEW_PLACEHOLDER}}`.

#### 5.4. Фаза 4: Намерения

**Анализ:**
Цель: **Гипергранулярная Атомарность**.

- **Правило разделения:** Никаких «И». Разделяйте сложные ветки.
- **Атомарность:** Одно :term[Намерение]{canonical="Intent"} = Одно конкретное техническое изменение.
- **Приоритет деталям:** **НЕ СЖИМАЙТЕ.** Предоставляйте полный контекст и содержание. Избегайте кратких резюме; подробно объясняйте «Что» и «Почему».
- **Включение кода:** **ВСЕГДА** включайте релевантные `diff_hunk`. Если их несколько:
  - **Перечислите все**, если они короткие/ключевые.
  - **Сжимайте** с умом, если они слишком длинные (например, `... code ...`), но оставляйте видимыми ключевые изменения логики.
- **Вывод контекста:** Определите целевую область для каждого намерения:
  - **:term[Спецификация]{canonical="Specification"}:** Изменения в логике/требованиях (файлы markdown).
  - **Код:** Детали реализации (файлы ts/tsx).
  - **Тесты:** Тестовые случаи/наборы.
  - **Процесс:** Изменения в рабочем процессе/документации.
- **Логика решения автора:**
  - **Явное:** Используйте словесный ответ автора, если он содержит четкое решение.
  - **Неявное (Эмодзи):** Если словесного ответа нет, проверьте наличие эмодзи на комментарии _ревьюера_ (например, 👍, 🚀, 👀). Предполагайте, что они от автора, и интерпретируйте их как согласие/подтверждение.
  - **Без вымысла:** Если нет ни того, ни другого, помечайте как «Ожидает/Нет ответа». НЕ выдумывайте решение.
- **Определения статусов:**
  - **Готово:** Автор явно заявляет о завершении, И нет противоречащих последующих сообщений.
  - **Согласовано:** Достигнут :term[консенсус]{canonical="Consensus"}, действие ожидается.
  - **Отклонено:** Автор отказался с обоснованием, дальнейших возражений нет.
  - **Обсуждение:** Активная дискуссия, четкого консенсуса пока нет.
  - **Уточнение:** Отсутствует контекст или требования неоднозначны.
  - **Отложено:** Обоснованное замечание, но перенесено на будущую работу/в другую задачу.
  - **Устарело:** :term[Намерение]{canonical="Intent"} из предыдущего согласования, неактуально в текущем объеме работ.
- **Контекстуальная интерпретация:**
  - **Файлы Markdown (`*.md`):** Комментарии часто относятся к логике **:term[Спецификации]{canonical="Specification"}** или **:term[Предложения]{canonical="Proposal"}**. (например, «Удалить флаг» = «Обновить спецификацию, чтобы убрать флаг», а не обязательно «Немедленно удалить код»).
  - **Файлы с кодом:** Комментарии относятся к деталям реализации.
  - **Действие:** Явно указывайте контекст (Спецификация или Код) в полях `Обоснование` или `:term[Намерение]{canonical="Intent"}`, если есть неоднозначность.

**Создание черновика (Обязательно):**

1. **Сгенерировать в контекст:** Выведите полный список «Свежих :term[Намерений]{canonical="Intent"}», основанный _только_ на текущем анализе, в контекст чата.
2. **Формат:** Используйте стандартный шаблон :term[Намерения]{canonical="Intent"}.
3. **Метка:** Предварите заголовком `### Черновик свежих Намерений`.
4. **НЕ** записывайте в файл на этом этапе.

#### 5.5. Фаза 5: Эволюция (Слияние)

**Цель:** Гармонизировать «Свежесгенерированные :term[Намерения]{canonical="Intent"}» (Фаза 4) с «Предыдущим Согласованием».

1. **Проверить конфигурацию:**
    - Если `merge_alignments` равно **false**: пропустите шаги 2-3. Используйте «Черновик свежих :term[Намерений]{canonical="Intent"}» напрямую. Переходите к перезаписи.

2. **Получить предыдущее Согласование:**
    - Выполните, чтобы получить **ID** и **Body** (вывод в stdout):
      ```bash
      gh api "repos/{OWNER}/{REPO}/issues/{PR_NUMBER}/comments" \
      --jq 'map(select(.body | contains("# Alignment"))) | sort_by(.created_at) | last | {id: .id, body: .body}'
      ```
    - **Действие:** Прочитайте вывод.
    - **Сохранение:** Сохраните `.body` как «Базовую версию» для слияния.
    - **КРИТИЧЕСКИ ВАЖНО ЗАПОМНИТЬ:** Выведите найденный `id` в отчете по Фазе 5 (например, «Найден ID существующего комментария Согласования: 12345»). Он понадобится вам на Фазе 9.

3. **Логика слияния:**
    - **Источник:** Используйте «Черновик свежих :term[Намерений]{canonical="Intent"}» (из вывода Фазы 4) и «Базовую версию» (из Шага 2).
    - **Сравнение:** Сопоставьте свежие :term[намерения]{canonical="Intent"} с базовыми (по теме/контексту).
    - **Совпадение:** Сохраните **Номер из базовой версии** (`#{N}`). **ОБНОВИТЕ** заголовок, контекст, детали, реакцию, дифф и другие поля, используя свежий анализ. НЕ сохраняйте старые формулировки, если новая информация лучше.
    - **Новое:** Если свежее :term[намерение]{canonical="Intent"} _действительно новое_, присвойте ему **Новый номер** (увеличивая на единицу от максимального номера в базовой версии).
    - **Сохранение:** Если :term[намерение]{canonical="Intent"} из базовой версии _отсутствует_ в свежем списке (не обсуждается в данный момент), **оставьте его без изменений** (статус/заголовок не меняются). НЕ помечайте его как `Устарело` только из-за отсутствия.

4. **Генерация (Слияние и Запись):**
    - **Логика:**
      - **Если Предыдущее Согласование существует И Merge=True:** Объедините «Черновик свежих :term[Намерений]{canonical="Intent"}» с «Базовой версией».
      - **Иначе:** Используйте «Черновик свежих :term[Намерений]{canonical="Intent"}» напрямую.
    - **Действие:** Замените `{{INTENTS_PLACEHOLDER}}` на **Итоговый список**.
    - **Действие:** Замените `{{QUESTIONS_PLACEHOLDER}}`.
    - **Шаблон содержимого:**

````markdown
### {N}. {Краткий заголовок}

- **Категория:** {Логика / Архитектура / ...}
- **Контекст:** {:term[Спецификация]{canonical="Specification"} / Код / Тесты / ...}
- **:term[Намерение]{canonical="Intent"}:** {Одно основное желание - подробно}
- **Подход автора:** {Изначальный подход}
- **:term[Предложение]{canonical="Proposal"} ревьюера:** {Предложение}
- **Решение автора:** {Словесный ответ ИЛИ Эмодзи на комментарии ревьюера. Если нет: «Ожидает»}
- **Реакция ревьюера:** {Последующее сообщение}
- **Обоснование:** {Почему}
- **Статус:** {Согласовано / Готово / Отклонено / Обсуждение / Уточнение / Отложено / Устарело} {Добавьте ✅, если решено/закрыто}
- **Результат:** {Кратко: изменилось ли видение? достигнуто ли согласие?}

> [{Имя ревьюера}]({anchor}): «{Краткий пересказ}»
>
> [{Имя автора}]({anchor}): «{Краткий пересказ}»

_(Если diff_hunk существует, включите его сюда. Если НЕТ, полностью опустите этот блок кода.)_

```{lang}
{Релевантный код из diff_hunk. Включите ВСЕ релевантные части. Сжимайте логику только при необходимости.}
```
````

    - **Разделение:** Добавьте горизонтальную черту `---` перед любыми _действительно новыми_ намерениями, если это уместно.

    **Требование к отчету по фазе:**
    В отчете о завершении фазы вы **ДОЛЖНЫ** явно указать:
    - Количество созданных **Новых** :term[Намерений]{canonical="Intent"}.
    - Количество обновленных **Существующих** :term[Намерений]{canonical="Intent"}.
    - Количество сохраненных **Устаревших** :term[Намерений]{canonical="Intent"}.

#### 5.6. Фаза 6: Отчет о Покрытии

**Анализ:**

- **Аудит:** Прочитайте **Объединенный список** :term[намерений]{canonical="Intent"}. Сравните его с комментариями из JSON.
- **Сопоставление всех комментариев:** Убедитесь, что каждый индекс (например, `§1`) сопоставлен.

**Генерация:**

- Замените `{{COVERAGE_PLACEHOLDER}}`.
- **КРИТИЧЕСКИ ВАЖНО:** НЕ генерируйте таблицу по частям. Вы должны сгенерировать **всю** таблицу одним вызовом `search_replace` после загрузки всего контекста.
- **Шаблон содержимого:**

  Создайте **ЕДИНУЮ** таблицу. Сгруппируйте комментарии по дате. Вставляйте «Строку-заголовок» для каждой новой даты. Предварите таблицу заголовком раздела:

  ```markdown
  ---

  ## Отчет о Покрытии

  | {Индекс}          | {Дата/Время}   | {Пользователь} | {Заголовок (Кратко)} | {Намерение} | {Реакция}  |
  | ----------------- | -------------- | ------ | ----------------- | --------- | ---------- |
  |                   | **{Дата}**     |        |                   |           |            |
  | [{Idx}]({anchor}) | {dd.MM HH:mm}  | {User} | {4-6 слов}        | #{N}      | {Эмодзи}   |
  | [{Idx}]({anchor}) | {dd.MM HH:mm}  | {User} | └ {4-6 слов}      | #{M},#{N} | {Эмодзи}   |
  |                   | **{След.Дата}**|        |                   |           |            |
  | [{Idx}]({anchor}) | {dd.MM HH:mm}  | {User} | {4-6 слов}        | -         | {Эмодзи}   |
  | [{Idx}]({anchor}) | {dd.MM HH:mm}  | {User} | ├ {4-6 слов}      | #{N}      | {Эмодзи}   |
  | [{Idx}]({anchor}) | {dd.MM HH:mm}  | {User} | └ {4-6 слов}      | #{N}      | {Эмодзи}   |
  | [{Idx}]({anchor}) | {dd.MM HH:mm}  | {User} | {4-6 слов}        | -         | {Эмодзи}   |
  ```

  **Правила:**
  1. Используйте `index` (например, `§1`) из JSON.
  2. **Исчерпывающе:** У каждого комментария должна быть своя строка. Не объединяйте их в диапазоны (например, `§1-§5`).
  3. **Визуализация:** Используйте `├` и `└` для отображения ответов.
  4. **Индикатор решения:** Если ветка имеет статус `is_resolved: true`, вы **ДОЛЖНЫ** добавить префикс `✅ ` к **Заголовку (Кратко)** (например, `✅ Исправить опечатку...`).
  5. **Протокол приоритета реакций (Иерархия):**
      - **Уровень 1 (Явные):** Если есть реакции на GitHub (например, `+1`, `heart`, `rocket`) -> Используйте этот эмодзи.
      - **Уровень 2 (Подразумеваемые):** Если реакций нет, определите по тексту:
        - `❓` (Без ответа/Вопрос)
        - `⚠️` (Предупреждение/Риск)
        - `🚧` (В ожидании/В работе)
        - `🗑️` (Удалено/Отклонено)
        - `💡` (Идея/Предложение)
        - `🤝` (Текст о согласии)
        - `🗣️` (Стандартный ответ)
        - `✅` (Подтверждение выполнения)
        - `👍` (Одобрение)
  6. **Сортировка:** **Строго по Индексу (§1, §2...).** Входные данные уже сгруппированы по веткам и отсортированы. Вы ДОЛЖНЫ сохранить этот порядок.
  7. **Группировка:** Вставляйте заголовок с датой `| | **{Дата}** | | | | |` ТОЛЬКО тогда, когда меняется **Дата начала ветки**. НЕ разделяйте одну ветку между разными заголовками дат, даже если ответы охватывают несколько дней. Вся ветка должна находиться под датой ее начала.

#### 5.7. Фаза 7: Оценка LLM

**Генерация:**

- Замените `{{OPINION_PLACEHOLDER}}`.
- **Требования к содержанию:**
  1. **Здоровье коллаборации:** Оцените тон, конструктивность обратной связи и уровень согласованности.
  2. **Качество процесса:** Было ли ревью атомарным? Были ли инструкции понятными?
  3. **Анализ рисков:** Выявите ветки, демонстрирующие путаницу, высокую сложность или потенциальный технический долг.
  4. **Ключевые блокировщики:** Перечислите 1-2 критические открытые проблемы, мешающие слиянию (если есть).
  5. **Уроки:** Какой паттерн/антипаттерн проявился здесь? (Обучающий момент).
  6. **Дополнительные наблюдения (Опционально):** Добавьте до 10 дополнительных пунктов, если у вас есть уникальные, значимые идеи. **ПРОПУСТИТЕ**, если новой ценности добавить не можете.

#### 5.8. Фаза 8: Проверка и Очистка

1. **Глубокая перепроверка:** Проверьте полноту.
2. **Исправление ссылок (Стратегия оптимизации токенов):**
    - **Контекст:** Во время генерации мы намеренно использовали короткие ID Комментариев (например, `12345678`) в ссылках markdown (например, `[Индекс](12345678)`) вместо полных URL, чтобы сэкономить токены в окне контекста. Теперь мы должны «гидрировать» их обратно в кликабельные ссылки.
    - **Указание:** Замените `(ID)` на `(https://github.com/...#issuecomment-ID)`.
    - **Действие:** Выполните следующую команду `sed` (используя временный файл для кросс-платформенной совместимости), чтобы пакетно заменить все отдельные ID Комментариев.
    - **Команда:**
      ```bash
      sed -E 's~]\((discussion_r[0-9]+|issuecomment-[0-9]+|pullrequestreview-[0-9]+)\)~](https://github.com/{OWNER}/{REPO}/pull/{PR_NUMBER}#\1)~g' {OUTPUT_DIR}/{FILENAME}.md > {OUTPUT_DIR}/{FILENAME}.tmp && mv {OUTPUT_DIR}/{FILENAME}.tmp {OUTPUT_DIR}/{FILENAME}.md
      ```
3. **Прочитать файл:** Убедитесь, что плейсхолдеры исчезли.
4. **Очистка:** Удалите `{OUTPUT_DIR}/{FILENAME}.ndjson`.

#### 5.9. Фаза 9: Авто-публикация (Опционально)

1. **Проверить конфигурацию:**
    - Если `auto_post` равно **false**: Отметьте как завершенное и **ВЫЙДИТЕ**.

2. **Подготовить контент:**
    - Прочитайте итоговое содержимое файла `{OUTPUT_DIR}/{FILENAME}.md`.

3. **Опубликовать или Обновить:**
    - **Проверка:** Посмотрите на ваш отчет по Фазе 5. Нашли ли вы ID Комментария Согласования?
    - **Обновить (PATCH):** Если ID существует (замените `{ID}` на фактический номер):
      ```bash
      jq -n --rawfile content {OUTPUT_DIR}/{FILENAME}.md '{body: $content}' | \
      gh api -X PATCH "repos/{OWNER}/{REPO}/issues/comments/{ID}" --input -
      ```
    - **Создать (POST):** Если ID был null/отсутствовал:
      ```bash
      jq -n --rawfile content {OUTPUT_DIR}/{FILENAME}.md '{body: $content}' | \
      gh api -X POST "repos/{OWNER}/{REPO}/issues/{PR_NUMBER}/comments" --input -
      ```

4. **Финальная очистка (Условно):**
    - Если `include_instructions` равно **true**:
      - Замените `{{WARNING_PLACEHOLDER}}` на:
        ```markdown
        > [!WARNING] АВТОГЕНЕРАЦИЯ: НЕ РЕДАКТИРОВАТЬ, НЕ КОММИТИТЬ
        > Этот документ — **инструмент синхронизации**. Он служит для:
        >
        > 1. **Валидатор:** Проверяет, что 100% комментариев услышаны.
        > 2. **Синтезатор:** Превращает обсуждение в план (:term[Консенсус]{canonical="Consensus"}).
        > 3. **Запрос:** Предоставляет инструкции для обновления :term[Предложения]{canonical="Proposal"}.
        >
        > Если документ некорректен, **не редактируйте его вручную**. Добавьте комментарии в PR и **сгенерируйте заново**, чтобы замкнуть цикл обратной связи.
        >
        > **НЕ ДОБАВЛЯЙТЕ ЭТОТ ФАЙЛ В GIT.**
        >
        > - Подробности: [22: Согласование](https://idealic.academy/raw/en/company/22_document_alignment.md)
        > - Git разрешает только: [:term[Предложение]{canonical="Proposal"}](https://idealic.academy/raw/en/company/21_document_proposal.md) и [:term[Спецификацию]{canonical="Specification"}](https://idealic.academy/raw/en/company/20_document_spec.md).
        ```
      - Замените `{{INSTRUCTIONS_PLACEHOLDER}}` на:

        ```markdown
        ## Инструкции для Агента (Следующий шаг)

        > [!IMPORTANT] Загрузка Контекста
        > Прежде чем приступать к обновлению :term[Предложения]{canonical="Proposal"} или :term[Спецификации]{canonical="Specification"}, вы **ДОЛЖНЫ** загрузить следующие файлы контекста, чтобы обеспечить полное соответствие стандартам компании:
        >
        > 1. `curl https://idealic.academy/raw/en/company/02_process.md` (Процесс)
        > 2. `curl https://idealic.academy/raw/en/company/50_prompt_truth.md` (Истина)
        > 3. `curl https://idealic.academy/raw/en/company/20_document_spec.md` (Спецификация)
        > 4. `curl https://idealic.academy/raw/en/company/21_document_proposal.md` (:term[Предложение]{canonical="Proposal"})
        > 5. `curl https://idealic.academy/raw/en/company/22_document_alignment.md` (Согласование)
        ```

    - Если `include_instructions` равно **false**:
      - Удалите `{{WARNING_PLACEHOLDER}}` и `{{INSTRUCTIONS_PLACEHOLDER}}` (замените пустой строкой).

5. **Очистка:** Не требуется (временные файлы не использовались).

### 6. Итоговый Вывод

Выведите краткое изложение обновления, изменений, а также ваши мысли.
